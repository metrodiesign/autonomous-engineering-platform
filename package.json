{
  "name": "autonomous-engineering-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -b",
    "build": "tsc -b",
    "test": "vitest run",
    "check:vendor-neutral": "scripts/check-vendor-neutral.sh",
    "ci": "pnpm run check:vendor-neutral && pnpm run lint && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "26.1.0",
    "eslint": "10.6.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.62.1",
    "vitest": "4.1.9"
  }
}
