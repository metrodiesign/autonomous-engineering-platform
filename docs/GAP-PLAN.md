# GAP-PLAN — spec audit 2026-07-05 (72-agent adversarial audit)

Confirmed gaps: 63 — close plan per item from the verifying agent. Mark [x] when closed.

## G01 [§10.1] — การวัด: injection canaries (runtime tripwire + P7 → susceptibility-aware routing)

**Why:** ยืนยันตามที่ auditor อ้างทั้งสองข้อ: (1) checkInjectionCanary (core/src/security-plane.ts:7) มีผู้เรียกเพียง core/test/phase2.test.ts — เส้นทาง runtime เดียวที่เห็น structuredResult คือ aal/src/bridge.ts:51-55 ซึ่งอ่านแค่ claim/note โดยไม่ตรวจ canary ดังนั้น tripwire ไม่ทำงานใน propose/dispose loop จริง; (2) aal/src/router.ts ตัดสินจาก capability+breaker เท่านั้น, grep "susceptib" ทั้ง repo เจอแค่ string ใน log note — ไม่มี susceptibility signal เข้า routing. ไม่ใช่ documented cut: DEVIATIONS.md

**Plan:** ขั้นต่ำที่ agent ทำได้เองโดยไม่ต้องมีมนุษย์: (1) Wire tripwire — เพิ่ม parameter (EventLog + optional onCanaryTrip callback) ให้ adapterAgentPort ใน aal/src/bridge.ts แล้วเรียก checkInjectionCanary(log, task.taskId, res.structuredResult) หลัง adapter.invoke; ถ้า tripped ให้คืน Proposal แบบ quarantine (actions: [], ไม่มี claim, note ระบุ canary trip) เพื่อไม่ให้ executor รัน action จากผลที่ปนเปื้อน — event INJECTION_CANARY_TRIPPED ถูก append โดยฟังก์ชันเดิมอยู่แล้ว; อัปเดต call site ที่สร้าง bridge (orchestrator/loop scripts) ให้ส่ง log เข้าไป. (2) Susceptibility-aware routing — เพิ่ม field susceptibility (ตัวนับ trip ต่อ adapter เช่น { trips: number; invocations: number }) ใน RouteEntry ที่ aal/src/router.ts แล้วใน route() เรียง capable entries จาก susceptibility rate ต่ำไปสูงก่อน loop breaker.admit(); จุด wire คือ onCanaryTrip จากข้อ (1) increment counter ของ adapter นั้น และ seed ค่าเริ่มจากผล P7 conformance (fail = trips+1). (3) Tests — bridge test: adapter mock คืน {echo:'CANARY-9911-TRIPPED'} → proposal ไม่มี actions + event log มี INJECTION_CANARY_TRIPPED; router test: adapter สองตัว capable เท่ากัน ตัวที่ trip มากกว่าถูกเลือกทีหลัง. (4) อัปเดต PROGRESS.md checklist Phase 2 ให้สะท้อนว่า canary ปิดครบ (tripwire wired + susceptibility routing) — ประมาณ 3 ไฟล์ source + 2 test files, ไม่แตะ spec/invariants

## G02 [§10.1] — Residual: backdoor เนียนใน L0–L2 auto-merge ยังเป็นไปได้ → sampling audit + escape-rate metric + human L3

**Why:** Confirmed gap. Spec §10.1 (unified-platform-spec.md:395) requires sampling audit + escape-rate metric + human L3 as residual mitigation, and §12 (:464) lists escape rate as a component metric; PROGRESS.md claims Phase 0-4 done so this is normative. Two legs exist: sampling audit (core/src/merge-policy.ts auditSampleRate/sampledForAudit; core/src/auditor.ts emitting AUDIT_REPRODUCED/AUDIT_NON_REPRODUCIBLE; run in scripts/run-semiauto-loop.mjs; tested in core/test/phase2.test.ts and phase3.test.ts

**Plan:** Smallest close: (1) Add escapeRate(log: EventLog) to core/src/auditor.ts (or new core/src/metrics.ts, exported from core/src/index.ts): scan the event log — denominator = MERGE_DECISION events with action 'auto-merge' and sampledForAudit=true, numerator = AUDIT_NON_REPRODUCIBLE events for those tasks; return { sampled, escaped, rate, interval } where interval is a wide Wilson/binomial range since §12 mandates reporting a range at small n. (2) Print the metric at the end of scripts/run-semiauto-loop.mjs after the audit step. (3) Add a unit test in core/test/phase3.test.ts: log one reproduced + one non-reproducible sampled audit, assert sampled=2, escaped=1, rate=0.5 and that unsampled/needs-review tasks are excluded. No human input needed; ~40 lines total.

## G03 [§10.1] — evidence ผลิตโดย core เท่านั้น (จับ output จาก child process ตรง + sign)

**Why:** Confirmed. §10.1 tier-1 Deterministic requires evidence "จับ output จาก child process ตรง + sign" with T6 evidence spoofing as a named threat, and PROGRESS.md claims all phases 0-4 built to spec. Capture+hash exist (core/src/executor.ts line 118 resultHash sha256 of stdout+stderr+exitCode) but repo-wide grep (core/aal/adapters/console/scripts/spikes/test) for hmac|createSign|signature|hash-chain|prev_hash finds signing only in console/backend/src/auth-provider.ts (Console login session tokens — 

**Plan:** Add a tamper-evident hash chain + HMAC to the event log, all agent-doable: (1) key management — on core startup generate a 32-byte key via crypto.randomBytes at .ai/runtime/evidence.key with mode 0600 if absent, add path to .gitignore; (2) core/src/event-log.ts — add prev_hash TEXT and sig TEXT columns (ALTER TABLE IF missing); in append(), compute entryHash = sha256(ts|task_id|type|principal|payload|prev_hash-of-last-row) and sig = HMAC-SHA256(key, entryHash), store both; (3) add verifyChain(db, key) that walks rows in seq order recomputing chain + HMAC and returns the first broken seq (detects edits, deletions, and reordering); call it on core startup (fail loud / emit integrity event) and expose as scripts/verify-event-log.ts; (4) tests in core: append N events then verifyChain passes; tamper one payload via raw better-sqlite3 UPDATE then verifyChain reports that seq; DELETE a middle row then verify fails — proving T6 spoofing via direct DB edit is now detectable.

## G04 [§10.2] — Rate limit ต่อ provider (token bucket); scheduler เคารพก่อน dispatch

**Why:** Gap จริง: §10.2 กำหนด "Rate limit ต่อ provider (token bucket); scheduler เคารพก่อน dispatch" เป็นข้อแยกจาก breaker/quota-probe ในหัวข้อเดียวกัน และอยู่ใน scope Phase 2 ซึ่ง PROGRESS.md ติ๊กว่าเสร็จแล้ว แต่ grep bucket/refill/throttle/limiter ใน aal/src+test, core/src+test, adapters/src ไม่พบ implementation ใด ๆ — aal/src/router.ts route() เช็คแค่ capability + breaker.admit() (error-rate breaker + quota probe ซึ่งเป็นกลไกคนละ bullet ตามสเปก) และ core/src/orchestrator.ts dispatch ตรงโดยไม่มี throt

**Plan:** เพิ่มไฟล์ aal/src/rate-limit.ts: class TokenBucket { constructor(key, {capacity, refillPerSec, now?}) ; tryTake(): boolean } refill แบบ lazy จาก timestamp (แพทเทิร์นเดียวกับ CircuitBreaker ที่รับ now() injectable เพื่อ test) · เพิ่ม optional field bucket?: TokenBucket ใน RouteEntry (aal/src/router.ts) แล้วแก้ route() ให้ entry ที่ bucket ไม่มี token ถือว่าไม่ eligible (ข้ามไป entry ถัดไป) และ consume token เฉพาะ entry ที่ถูกเลือกจริงก่อน return — ทำให้ scheduler/router เคารพ rate limit ก่อน dispatch ตามสเปก; ไม่มี eligible เหลือ → คง BLOCKED(no_capacity) เดิม (INV-5 ไม่วนยิง) · ค่า default ต่อ provider อ่านจาก manifest หรือ config ที่จุดสร้าง RouteEntry (เช่น capacity 5, refill 0.5/s — ค่าเล็ก+ปรับได้, บันทึกเป็น decision ใหม่ใน docs/DECISIONS.md ให้มนุษย์ทบทวน) · เพิ่ม test ใน aal/test: (1) bucket หมด → route ข้ามไป adapter ถัดไป (2) ทุกตัวหมด → no_capacity (3) refill ตามเวลาแล้ว admit ใหม่ (4) token ถูก consume เฉพาะ entry ที่ถูกเลือก · รัน pnpm test + lint ให้ CI เขียว · commit บน branch build/platform (ห้าม push main)

## G05 [§10.2] — 

**Why:** Confirmed. (1) POST /kill (core/src/human.ts:152) only logs KILL_SWITCH and calls optional onKill; no caller ever supplies onKill — scripts/run-supervised-loop.mjs:95 calls startHumanPlane({log, approvals, tokenFile}) with no onKill/onSteer, so kill is a logged no-op. (2) Zero credential-revoke or worktree-quarantine code exists (quarantine hits are only the QUARANTINED TaskState for flaky tests and a canary comment; rollback-worktree.sh is §14 checkpoint rollback, not kill quarantine). (3) PAUS

**Plan:** 1) core/src/human.ts: extend HumanPlaneDeps with steering?: SteeringController and add endpoints POST /steering/pause {taskId} → steering.requestPause, POST /steering/resume {taskId} → steering.resume, and make POST /steer delegate to steering.inject when onSteer absent (keep /steer as inject alias per §10.3). 2) New core/src/kill.ts: makeKillHandler({children, worktreeDir, tokenFile, log}) that (a) SIGTERM+SIGKILL all tracked child processes / abort AbortControllers, (b) quarantines the worktree by renaming it to `${worktreeDir}.quarantined-<ts>` and writing a QUARANTINE.json marker, logging WORKTREE_QUARANTINED, (c) revokes all platform-managed credentials: delete/rotate the Human Plane token file and any console session tokens, logging CREDENTIAL_REVOKED; for vendor credentials the platform does not own (Claude subscription), log ESCALATED(revoke_vendor_credentials) — that part inherently needs a human, document it in DEVIATIONS.md as DEV-002. 3) Wire it: scripts/run-supervised-loop.mjs (and any long-running loop entrypoint) pass onKill=makeKillHandler(...) and steering=new SteeringController(log) into startHumanPlane; console/backend/src/loop-console.ts add proxies /api/loop/pause and /api/loop/resume → /steering/pause|resume with audit events, and hook web.ts F-Loop UI. 4) Tests (core/test): POST /steering/pause then /steer succeeds and /steering/resume emits RESUMED; POST /kill kills a spawned dummy child, token file gone, worktree dir renamed with marker; loop-console proxy test for pause/resume. 5) Update PROGRESS.md checkpoint noting kill semantics + pause API closed.

## G06 [§10.3] — Human Plane API: POST /steering/{pause·inject·resume} ... Steering: PAUSE_REQUESTED → GUIDANCE_INJECTED → RESUMED

**Why:** Confirmed. Spec §10.3 (unified-platform-spec.md:408-410) normatively requires Human Plane endpoints POST /steering/{pause·inject·resume} and the flow PAUSE_REQUESTED→GUIDANCE_INJECTED→RESUMED; steering is Phase 2 and PROGRESS.md claims Phases 0-4 done. core/src/steering.ts implements the state machine correctly (tested in core/test/phase2.test.ts:57), but core/src/human.ts exposes only POST /steer (inject-shaped, via optional onSteer hook) — no pause/resume routes exist anywhere. The Steering cl

**Plan:** 1) core/src/human.ts: add `steering?: Steering` to HumanPlaneDeps and three routes — POST /steering/pause {taskId} → steering.requestPause → 200; POST /steering/inject {taskId, guidance, touchesAcOrScope} → steering.inject → 200 with asDataPiece, or 409 with reason (not paused / needs contract amendment); POST /steering/resume {taskId} → steering.resume → 200. Return 501 on all three when steering is not provided. Rewire existing POST /steer to steering.inject(taskId,{text:guidance,touchesAcOrScope:false}) when steering is present (keeps Console client working; keep onSteer fallback for compat). 2) scripts/run-supervised-loop.mjs: construct `const steering = new Steering(log)` sharing the run's EventLog, pass it into startHumanPlane, and in the per-task iteration loop check steering.isPaused(t.id) — if paused, wait/poll before dispatching the next iteration, and append any accepted guidance asDataPiece into the agent context as marked data. 3) console/backend/src/loop-console.ts: add proxy routes POST /api/loop/pause and /api/loop/resume → Human Plane /steering/pause|resume with audit events; web.ts F-Loop: add Pause/Resume buttons next to Steer. 4) Tests (core/test/phase2.test.ts or new core/test/human-steering.test.ts, HTTP-level against startHumanPlane): inject before pause → 409; pause → inject → 200 + GUIDANCE_INJECTED event; touchesAcOrScope=true → 409 + GUIDANCE_REFUSED_NEEDS_AMENDMENT; resume → RESUMED event; endpoints without steering dep → 501. Run pnpm test + CI locally on branch build/platform.

## G07 [§10.3] — Escalation ต้องตัดสินได้: จบด้วยคำถาม + ตัวเลือกพร้อมราคา/ความเสี่ยง (จาก hypothesis log) — ห้ามส่งกอง log

**Why:** ยืนยัน gap จริง. หลักฐานตรวจซ้ำแล้ว: (1) core/src/orchestrator.ts:37 transition เป็น ESCALATED เมื่อ BUDGET_EXCEEDED โดยไม่สร้าง package ใด ๆ; (2) core/src/repair.ts:45 คืน {confirmed:null, refuted:[statement...], escalate:true} — refuted เป็นแค่ string list ไม่มีโค้ดแปลงเป็นคำถาม+ตัวเลือกพร้อมราคา/ความเสี่ยง; (3) core/src/human.ts คือ implementation ของ §10.3 ทั้งไฟล์ (ApprovalStore + endpoints /approvals, /steer, /kill, /events) แต่ไม่มี EscalationPackage type, ไม่มี endpoint /escalations, ไม่

**Plan:** งานเล็กสุดที่ agent ทำได้เองครบวงจร: (1) สร้าง core/src/escalation.ts — type EscalationPackage { id, taskId, reason: 'budget_exceeded'|'hypotheses_exhausted', question: string, options: [{ label, action, estimatedCost, risk }], refutedSummary: string[], createdAt, status:'pending'|'resolved' } + ฟังก์ชัน deterministic buildEscalationPackage(log: EventLog, taskId, reason) ที่อ่าน event ของ task (HYPOTHESIS_REFUTED, HYPOTHESES_TRUNCATED, BUDGET_EXCEEDED, GATES_RUN detail ล่าสุด) แล้วสรุปเป็นคำถามเดียวที่ตัดสินได้ + ตัวเลือกมาตรฐาน: retry-with-bigger-budget (cost=iterations/quota เพิ่ม, risk=เปลืองโควตา), split-task (cost=replan, risk=ต่ำ), cancel (cost=งานที่ทำมา, risk=ไม่มี) — และถ้า hypothesis ถูก truncate ให้เพิ่มตัวเลือก continue-diagnosis พร้อม estimatedBlastRadius จาก ifConfirmed เป็น risk; ห้ามใส่ raw event array ลง package (บังคับ "ห้ามส่งกอง log") + EscalationStore แบบเดียวกับ ApprovalStore (create/listPending/resolve(id, chosenOptionIndex) + log ESCALATION_RAISED/ESCALATION_RESOLVED). (2) wire: orchestrator.ts จุด BUDGET_EXCEEDED และ caller ของ runHypothesisProbes เมื่อ escalate:true → store.create(buildEscalationPackage(...)). (3) core/src/human.ts เพิ่ม GET /escalations + POST /escalations/{id} (body {option: number}) mirror pattern ของ /approvals. (4) Console F-Loop (console/backend SPA) เพิ่ม section Escalations: แสดง question + ปุ่มต่อ option พร้อม cost/risk แล้ว POST กลับ. (5) tests ใน core/test: unit — seed event log ที่มี refuted 3 ตัว แล้ว assert package จบด้วย '?'/มี options ทุกตัวมี estimatedCost+risk/ไม่มี field ที่เป็น raw events; integration — รัน orchestrator ให้ budget exceeded แล้ว GET /escalations เจอ package pending และ POST resolve ได้. ทั้งหมดไม่ต้องการ input จากมนุษย์เพราะ template คำถาม/ตัวเลือกกำหนดจากข้อมูลใน hypothesis log ตามที่ §10.3 ระบุเอง

## G08 [§11.1] — 

**Why:** ยืนยัน gap จริง: ไม่มีไฟล์ .ai/goal.yaml ที่ไหนใน repo (find ทั้ง repo ว่าง); ไม่มี loader/parser/schema ของ Goal Contract — จุดเดียวที่อ้างถึงคือ console/backend/src/server.ts:152-153 ซึ่งแค่ existsSync เพื่อโชว์ loop-managed banner; core/src/planning-gate.ts ใช้ GoalContractLite ที่มีแค่ acceptanceCriteria ids (สร้าง inline ใน core/test/phase4.test.ts เท่านั้น); loop scripts (run-supervised-loop.mjs:22-73, run-semiauto-loop.mjs:40-45, run-fusion-measure.mjs:51) hardcode goal/AC inline; กลไก ve

**Plan:** แผนปิดเล็กสุด (agent ทำได้เองทั้งหมด): (1) เพิ่ม core/src/goal-contract.ts — type GoalContract เต็มตาม §11.1 (goal, business_outcomes, scope{include,exclude}, constraints{stack,forbidden}, acceptance_criteria[{id,description,verification,golden?}], quality_gates, budget, approval_policy{require_human_approval}) + validateGoalContract(obj): ตรวจ field บังคับ/ชนิด + AC id ไม่ซ้ำ — core รับ parsed object (vendor-free, ไม่ผูก YAML lib); (2) เพิ่ม versioned amendment ใน core: contract มี version:number + amendGoalContract(current, amendment) คืน contract ใหม่ version+1 พร้อม append event ลง event log (INV-10) — การแก้ AC/scope โดยไม่ผ่าน amendment ให้ validate fail (frozen semantics) และชี้ steering.ts:29 มาที่ path นี้; (3) loader ฝั่งนอก core: scripts/load-goal-contract.mjs อ่าน .ai/goal.yaml ด้วย package `yaml` (ISC, maintained — บันทึก review ตาม dependency rules, commit lock file) → validate ด้วย core → ส่งเข้า checkPlanningGate (GoalContractLite เป็น structural subset อยู่แล้ว ไม่ break); ให้ run-semiauto-loop.mjs/run-supervised-loop.mjs ใช้ loader เมื่อไฟล์มี (fallback inline สำหรับ demo tasks เดิม); (4) สร้าง .ai/goal.yaml ตัวอย่างจริงของ platform เอง (fields ครบตาม §11.1, budget/approval_policy สอดคล้อง policy ปัจจุบัน) — ทำให้ F-Proj banner ที่ server.ts:152 ทำงานจริงด้วย; (5) tests ใน core/test: valid/invalid contract, amendment bump version + reject การแก้ AC โดยไม่ bump, planning gate consume contract เต็ม; (6) บันทึก checkpoint ใน PROGRESS.md + หมายเหตุใน docs/DECISIONS.md ว่า dependency `yaml` ถูกเพิ่มพร้อมเหตุผล — ทั้งหมดไม่ต้องการ input จากมนุษย์

## G09 [§11.2] — 

**Why:** ยืนยันตามหลักฐานจริง: checkPlanningGate (core/src/planning-gate.ts) ถูกเรียกจาก core/test/phase4.test.ts เท่านั้น — grep ทั้ง repo ไม่พบ call site ใน orchestrator (core/src/orchestrator.ts มีแค่ runTask เดี่ยว), ไม่พบใน scripts/run-supervised-loop.mjs / run-semiauto-loop.mjs (รัน task hardcoded โดยไม่ผ่าน gate), ไม่พบใน console/aal/adapters; ไม่มี DAG/task-graph scheduler ใด ๆ (ไม่มี .ai/task-graph.json ตาม §14, ไม่มี deps-PASSED/lease/risk selection logic); และไม่ใช่ documented cut — PROGRESS.m

**Plan:** ขั้นต่ำสุดที่ agent ทำได้เองโดยไม่ต้องรอมนุษย์: (1) เพิ่ม core/src/planner.ts — type TaskGraph { tasks: (PlannedTask & { deps: string[]; risk: 'L0'|'L1'|'L2'|'L3'|'L4' })[] } + function selectReadyTasks(graph, passedTaskIds, opts { maxRisk, budgetAvailable, leasedTaskIds }): คืนเฉพาะ task ที่ deps ทุกตัวอยู่ใน passedTaskIds + risk <= maxRisk + budget available + ไม่ถูก lease; export ผ่าน core/src/index.ts. (2) เพิ่ม function loadTaskGraph(path) อ่าน .ai/task-graph.json (สร้าง sample file จาก tasks ที่ loop ใช้อยู่) แล้ว throw/block ถ้า checkPlanningGate(goal, graph.tasks).ok === false พร้อม append event PLANNING_GATE_BLOCKED ลง EventLog. (3) แก้ scripts/run-semiauto-loop.mjs และ run-supervised-loop.mjs ให้สร้าง goal contract + task list แล้วเรียก loadTaskGraph/checkPlanningGate ก่อน orchestrator.runTask — ถ้า gate ไม่ผ่านให้ exit non-zero. (4) เพิ่ม test ใน core/test/phase4.test.ts (หรือ planner.test.ts ใหม่): deps ไม่ PASSED → ไม่ถูกเลือก, gate block → loop ไม่รัน, gate ผ่าน → เลือกเฉพาะ READY. (5) อัปเดต PROGRESS.md บรรทัด Phase 4 ให้ตรงความจริง (wired ไม่ใช่แค่ pure function) — ไม่ต้องแตะ merge policy/lease กลไกอื่นที่มีอยู่แล้ว

## G10 [§11.3] — 

**Why:** .ai/schemas/ มีแค่ .gitkeep — ไม่มี schema file ทั้ง 7 ตัว (plan/task-result/review/failure/hypothesis/deliberation-analysis/approval-package); ไม่มีโค้ดใดอ้าง .ai/schemas/ (schema ที่ใช้จริงเป็น inline literal ใน aal/src/bridge.ts:14 + conformance.ts ส่งแบบ schema-in-prompt); ไม่มี runtime validation ต่อ schema (ไม่มี ajv/zod/validator ใดใน core/aal/adapters); ไม่อยู่ใน DEVIATIONS.md (มีแค่ DEV-001), DECISIONS.md, หรือ pending-human list ใน PROGRESS.md (5 ข้อ ไม่มีข้อนี้); §11.3 เป็น normative 

**Plan:** 1) สร้าง JSON Schema 7 ไฟล์ใน .ai/schemas/ (plan.json, task-result.json, review.json, failure.json, hypothesis.json, deliberation-analysis.json, approval-package.json) โดย derive จาก TS interface ที่มีอยู่แล้ว: Proposal/AgentResponse.structuredResult ใน aal/src/protocol.ts, ApprovalPackage ใน core/src/human.ts, types ใน core/src/types.ts (hypothesis จาก core/src/orchestrator.ts hypothesis-repair, review/failure จาก merge-policy/state-machine) — ใช้ JSON Schema draft-07 แบบ required+type ตรงกับ field จริง 2) เพิ่ม module เล็ก (เช่น core/src/schema.ts หรือ aal/src/schema.ts) loadSchema(name) + validate(value, schema) แบบ structural check (required keys + type) ~50 บรรทัด — ไม่เพิ่ม dependency ใหม่ (เลี่ยงกฎ dependency review) 3) ให้ aal/src/bridge.ts โหลด OUTPUT_SCHEMA จาก .ai/schemas/task-result.json แทน inline literal และเรียก validate ตอนรับ structuredResult (ต่อกับ bounded-repair path เดิม) ; จุด ingest อื่น (approval package ใน human plane, hypothesis ใน orchestrator) เรียก validate ก่อนใช้ 4) เพิ่ม test: (a) schema file ทั้ง 7 มีอยู่ + parse ได้ (b) fixture ตัวอย่างของแต่ละ type ผ่าน validate (c) fixture ที่ขาด required field ต้อง fail — รันใน CI เดิม (vitest) 5) อัปเดต PROGRESS.md บรรทัดสถานะว่าปิด §11.3 แล้ว

## G11 [§12] — 

**Why:** ยืนยัน gap จริง: (1) ไม่มี systemVersion = hash(core+policies+prompts) ที่ใดในโค้ด — grep core/aal/scripts/console = 0 hit; (2) ไม่มี scripts/calibrate.sh — ชื่อนี้โผล่จุดเดียวเป็น placeholder string ใน console/backend/src/web.ts:856 ชี้ไปสคริปต์ที่ไม่มีอยู่; (3) calibration records ทั้งสองไฟล์ใน .ai/calibration/ ไม่มี field system version → เทียบ baseline ข้ามเวอร์ชันไม่ได้; (4) ไม่มีกลไก compare baseline + block เมื่อถดถอยเกิน threshold; (5) pending-human ข้อ 1 ใน PROGRESS.md ครอบเฉพาะเฉลยมนุษ

**Plan:** การ implement เล็กสุดที่ agent ทำได้โดยไม่ต้องรอมนุษย์ (เฉลยจริงยังเป็น pending-human ตามเดิม): (1) เพิ่ม core/src/calibration.ts — pure functions: computeSystemVersion(roots=[core/src, .ai/policies, .ai/agents]) = sha256 ของ (relative path + file sha256) เรียง sort ทุกไฟล์, ตัดสั้น 16 hex; และ checkRegression(current, baseline, threshold) รับ heldOutPassed แบบ passed/total คืน {block: boolean, reason} — n เล็กให้เทียบแบบ interval ตามสเปก (block เมื่อ passed/total ต่ำกว่า baseline เกิน threshold ที่ config, default เช่น ห้าม pass rate ลดเลยเมื่อ n<=5); (2) เพิ่ม unit tests ใน core/test/calibration.test.ts: hash deterministic + เปลี่ยนไฟล์ใน core/src แล้ว hash เปลี่ยน + เปลี่ยนไฟล์นอก roots แล้ว hash ไม่เปลี่ยน + regression block/pass cases; (3) สร้าง scripts/calibrate.sh (wrapper เรียก scripts/calibrate.mjs): คำนวณ systemVersion → รัน calibration suite เดิมผ่าน run-supervised-loop.mjs กับ CAL-* tasks (model=haiku เช่นเดียวกับ bootstrap) → เขียน record ใหม่ .ai/calibration/cal-<date>-<systemVersion>.json ที่มี field systemVersion + heldOutPassed → หา baseline = record ล่าสุดที่มี systemVersion (ถ้าไม่มีให้ถือ run นี้เป็น baseline แรก) → ถ้า checkRegression.block = exit 1 พร้อมข้อความ "regression exceeds threshold — upgrade blocked"; รองรับ --hash-only สำหรับใช้ก่อนอัปเกรดโดยไม่เผาโควตา; (4) backfill field systemVersion (คำนวณจาก tree ปัจจุบัน + หมายเหตุ retroactive) ลง bootstrap-haiku-2026-07-04.json เพื่อให้มี baseline ใช้เทียบ; (5) อัปเดต PROGRESS.md ระบุกลไกเสร็จแล้ว เหลือเฉพาะเฉลยมนุษย์ตาม pending-human ข้อ 1 เดิม — placeholder ใน F-Sched (web.ts:856) จะชี้สคริปต์ที่มีอยู่จริงโดยไม่ต้องแก้ console

## G12 [§13.1] — รัน interactive + ไม่มี provider → เสนอ setup ทันที (basic/OIDC)

**Why:** ยืนยันว่า gap จริง. สเปก §13.1 บรรทัด 477 บังคับ "รัน interactive + ไม่มี provider → เสนอ setup ทันที (basic/OIDC)" แต่ (1) console/backend/bin/platform.mjs เป็น entrypoint เดียว รับเฉพาะ cmd 'console' และเรียก decideStartup({ hasAuthProvider: false }) แบบ hardcode — non-loopback ไม่มี provider จะ console.error + exit(1) เท่านั้น ไม่มี TTY check / prompt / setup flow ใด ๆ (grep ทั้ง repo รวม test/, scripts/, docs/ ไม่พบ) ยิ่งกว่านั้น hardcode false ทำให้แม้ตั้ง env PLATFORM_CONSOLE_PASSWORD_RECO

**Plan:** แก้ที่ console/backend/bin/platform.mjs + src/gate.ts เป็นหลัก: (1) เพิ่ม subcommand `platform auth set-password` ใน platform.mjs — อ่าน password จาก stdin (readline, ปิด echo), เรียก hashPassword() จาก dist/auth-provider.js, gen signingSecret ด้วย randomBytes(32).toString('hex'), persist ลงไฟล์ config เช่น ~/.platform/console-auth.json (chmod 600) ในรูป { passwordRecord, signingSecret }. (2) ให้ server.ts อ่าน record จากไฟล์นี้เป็น fallback เมื่อไม่มี env สองตัว (env ยังชนะ). (3) ใน `platform console`: คำนวณ hasAuthProvider จริงจาก env/ไฟล์ config แทน hardcode false; ถ้า gate ON + ไม่มี provider + process.stdin.isTTY && process.stdout.isTTY → พิมพ์ข้อเสนอ "no auth provider — set up Basic auth now? [y/N]" (พร้อม note ว่า OIDC config ได้ภายหลังตาม D-002) → ถ้า y รัน set-password flow inline แล้ว start ต่อ; ถ้า N หรือ non-TTY → fail-closed เดิม (สเปกบรรทัด 478 คงไว้). (4) อัปเดต error message ใน gate.ts ให้ชี้คำสั่ง `platform auth set-password` และตัดข้อความ "available from Phase 3" ที่ stale. (5) tests ใน console/backend/test/gate.test.ts + ไฟล์ใหม่: hashPassword→record ไฟล์ถูก format, provider-detection จาก env/ไฟล์, non-TTY ไม่มี provider ยัง exit non-zero, TTY-prompt logic แยกเป็น pure function (รับ { isTTY, hasProvider, gateOn } คืน action) เพื่อ test ได้โดยไม่ต้อง spawn PTY. (6) อัปเดต PROGRESS.md checklist §13.3 + comment ใน auth-provider.ts:6 ให้ตรงความจริง. ไม่ต้องสร้าง OIDC provider — D-002 บันทึกไว้แล้วว่า Basic ก่อน OIDC ทีหลัง แค่ให้ prompt เอ่ยถึงทางเลือก OIDC พอ.

## G13 [§13.1] — bind อื่น และไม่มี --insecure → gate ON (และ gate ON + มี provider → start ได้)

**Why:** Confirmed. platform.mjs:19 hardcodes hasAuthProvider:false and never reads PLATFORM_CONSOLE_PASSWORD_RECORD/PLATFORM_CONSOLE_SIGNING_SECRET that server.ts:84-87 uses to activate BasicAuthProvider, so the §13.1 branch "gate ON + provider → start" is unreachable via the normative `platform console` launcher (§8); git history shows platform.mjs untouched since Phase 0 (0a28626) while Phase 3 (2749c69) added the provider only in server.ts; the only hasAuthProvider:true call is in gate.test.ts; not o

**Plan:** 1) In console/backend/src/gate.ts add and export: hasConfiguredAuthProvider(env = process.env): boolean → Boolean(env.PLATFORM_CONSOLE_PASSWORD_RECORD && env.PLATFORM_CONSOLE_SIGNING_SECRET) — the exact same condition server.ts:85-86 uses. 2) In console/backend/bin/platform.mjs import it from '../dist/gate.js' and change line 19 to decideStartup({ host, insecure, hasAuthProvider: hasConfiguredAuthProvider(process.env) }); optionally also print a hint that remote peers additionally require PLATFORM_CONSOLE_HOST (peer-IP guard, server.ts:65-80). 3) Extend console/backend/test/gate.test.ts: hasConfiguredAuthProvider returns true with both vars set, false with either missing, and decideStartup(non-loopback, provider from env) → start:true. 4) Rebuild dist (pnpm -C console/backend build or tsc) since the launcher imports compiled dist/gate.js, then run the test suite. No human input needed.

## G14 [§13.1] — --insecure → ปิด gate + พิมพ์คำเตือน

**Why:** ยืนยันตามที่ auditor อ้างทุกจุด: (1) spec §13.1 (unified-platform-spec.md:479) เขียนชัดเป็น normative ว่า `--insecure → ปิด gate + พิมพ์คำเตือน` และ §13 เป็นขอบเขต Phase 3 ซึ่งสร้างแล้ว (PROGRESS.md ระบุ Phase 3 DoD ผ่าน พร้อม Basic auth provider ใน console/backend/src/auth-provider.ts) ดังนั้น requirement นี้ normative สำหรับ phase ที่ build แล้ว (2) ครึ่ง "พิมพ์คำเตือน" implement แล้วที่ console/backend/bin/platform.mjs:24 แต่ครึ่ง "ปิด gate" ไม่เคยเกิด: decideStartup ใน console/backend/src/ga

**Plan:** Implement ตามสเปก §13.1 โดยคง F-Term exception ของ §13.3/INV-17: (1) console/backend/src/gate.ts — เพิ่ม branch ใน decideStartup หลังเช็ค hasAuthProvider: `if (i.insecure) return { start: true, reason: 'non-loopback with --insecure — auth gate OFF by operator request (F-Term still requires auth per INV-17)' }` (2) console/backend/bin/platform.mjs — ส่งสถานะ insecure เข้า server เช่น set `process.env.PLATFORM_CONSOLE_INSECURE='1'` ก่อน buildServer() (หรือเพิ่ม param ใน buildServer deps) — warning ที่บรรทัด 24 มีอยู่แล้ว คงไว้ (3) console/backend/src/server.ts — กรณี insecure mode + ไม่มี provider: เพิ่ม preHandler เฉพาะ route F-Term (`/api/terminal*` + PTY WS upgrade path ใน terminal.ts) ให้ตอบ 401 เมื่อ peer ไม่ใช่ loopback เพื่อรักษา INV-17 "remote ต้องผ่าน auth ทุกกรณีรวม --insecure" (4) console/backend/test/gate.test.ts — แก้ test บรรทัด 19-22 เป็น expect start=true พร้อม reason ที่มีคำเตือน และเพิ่ม server-level test: buildServer แบบ insecure + no provider → route ทั่วไปเข้าถึงได้ แต่ /api/terminal จาก non-loopback peer ได้ 401 (5) อัปเดตบรรทัด comment หัวไฟล์ gate.ts/gate.test.ts ที่อ้างว่า INV-17 ครอบทั้ง gate ให้ถูกต้อง และเพิ่มบรรทัดสถานะใน PROGRESS.md Security checklist §13.3 (--insecure implemented ตาม §13.1, F-Term ยังถูก guard) — ทางเลือกที่เล็กกว่าแต่ต้องมนุษย์ยืนยันภายหลัง: เพิ่ม DEV-002 ใน docs/DEVIATIONS.md บันทึกว่าจงใจเข้มกว่าสเปก (--insecure = no-op จน Phase 3 provider path) พร้อมเหตุผลและวิธีพลิก — แนะนำทางแรกเพราะสเปกประกาศตัวเองเป็น source of truth และ deviation ประเภทนี้ไม่เข้า 3 หมวดที่ DEVIATIONS.md อนุญาต (CLI/SDK behavior, "ควร"-default, user-ordered)

## G15 [§13.3] — Consent gates (hooks, bypassPermissions — INV-16) ทำงาน

**Why:** Confirmed. Spec §13.3 (unified-platform-spec.md:493) requires consent gates for both hooks AND bypassPermissions (INV-16), and §13.3 is a release checklist for every phase (Phases 0-4 all claimed done). Hooks gate exists (console/backend/src/extensions.ts:84 returns 428, tested in extensions.test.ts:35 and phase5-console.test.ts:135). bypassPermissions gate does not exist anywhere: grep finds 'bypassPermissions' only in the spec, build-kickoff-prompt.md, spikes/RESULTS.md, and node_modules SDK t

**Plan:** Smallest fix, all in console/backend (mirror the existing hooks-gate pattern at extensions.ts:84): (1) In governance.ts PUT /api/settings/:scope, add Body field consent?: boolean; before safeWrite, detect risky payload — JSON.stringify(req.body.settings).includes('"bypassPermissions"') (covers permissions.defaultMode and any nested use) — and if risky && !consent return reply.code(428).send({ error: 'consent required: bypassPermissions disables all permission prompts' }). (2) In governance.ts PUT /api/permissions/:scope, add consent?: boolean; compute currentDeny from the existing file and if req.body.deny is provided and omits any rule in currentDeny (deny-loosening per INV-16) && !consent → 428 'consent required: removing deny rules loosens gates'. (3) Include consented flag in the existing audit() events for both writes. (4) In web.ts F-Set/F-Perm pages, on a 428 response show confirm() with the error text and retry with consent:true (matches existing consent-checkbox UX). (5) Add tests in console/backend/test/governance-consent.test.ts styled after extensions.test.ts: settings write with defaultMode=bypassPermissions no-consent → 428, with consent → 200; permissions write removing an existing deny rule no-consent → 428, with consent → 200; benign settings write and deny-tightening write pass without consent; audit entries recorded. (6) Update PROGRESS.md §13.3 status line to read 'consent gates hooks + bypassPermissions/deny-loosening (428, มี test)'. No human input needed.

## G16 [§13.3] — Endpoint spawn run (`/api/runs`, `/api/jobs`, `/hooks/in/*`) + Human Plane API มี rate limit + token

**Why:** ยืนยันตามหลักฐาน: /api/sched/start (console/backend/src/loop-console.ts:61-77) เป็น endpoint spawn process ตามอำเภอใจ แต่มีแค่ automation quota guard (429 เมื่อ utilization >= 0.85) + 409 ชื่อซ้ำ — ไม่มี rate limit ใด ๆ; เมื่อ utilization ต่ำสามารถยิง start ชื่อใหม่รัว ๆ ได้ไม่จำกัด (process exhaustion) ขณะที่ endpoint เสี่ยงอื่นมี rate limit ครบ (login 5/min ที่ auth-provider.ts:31, PTY 5/10s ที่ pty-manager.ts:41-50, Human Plane token+200/10s ที่ core/src/human.ts:98-107) และ PROGRESS.md:77 เอ

**Plan:** แก้เล็กสุดตาม pattern ที่มีอยู่แล้วใน repo: (1) ใน console/backend/src/loop-console.ts เพิ่ม sliding-window limiter แบบเดียวกับ PtyManager — เก็บ startTimes: number[] ใน closure ของ registerLoopConsole, ใน handler /api/sched/start กรอง timestamp อายุ < 10_000ms แล้วถ้า length >= 5 ตอบ 429 { error: 'sched start rate limit exceeded (§13.3)' } ก่อนเช็ค quota guard (ประมาณ 5-6 บรรทัด); (2) เพิ่ม test ใน console/backend/test/phase3.test.ts: inject POST /api/sched/start 6 ครั้งด้วยชื่อต่างกัน (cmd ใช้ /bin/sleep หรือ mock) แล้ว assert ครั้งที่ 6 ได้ 429; (3) อัปเดต PROGRESS.md:77 เป็น "rate limit (login 5/min, PTY 5/10s, sched 5/10s, Human Plane 200/10s)"; (4) รัน test suite เดิมให้เขียวทั้งหมด ไม่แตะไฟล์อื่น

## G17 [§13.3] — Redaction ครอบทุก response/log + path credential files (INV-14, มี test)

**Why:** ยืนยัน gap จริง: (1) ข้อกำหนดเป็น normative — §13.3 บรรทัด 488 + INV-14 บรรทัด 80 ระบุชัด "Redaction ครอบทุก response/log ของ Console + path ของ credential files (มี test)" และ Phase 3 DoD บังคับ "§13.3 ครบ" ซึ่ง PROGRESS.md mark ผ่านแล้ว (2) implementation มีแค่จุด ๆ: auth.ts ไม่คืนค่า secret (auth.test.ts:22), context-builder block-not-redact (core path), audit log ไม่ใส่ payload — ไม่มี central redaction sweep (onSend hook/serializer) และไม่มี test ระดับ response เลย (3) จุดรั่วยืนยันได้ 2 จุ

**Plan:** สร้าง console/backend/src/redact.ts: (ก) redactDeep(value) — recursive walk JSON: mask ค่าของ key ที่ match /(token|secret|password|api[_-]?key|authorization|credential)/i และ string value ที่ match รูปแบบ secret รู้จัก (sk-ant-*, sk-*, ghp_/gho_/github_pat_*, AKIA*, xox[baprs]-*, "Bearer <jwt/opaque>", eyJ...JWT, PEM private key block) → แทนด้วย sentinel "[REDACTED]" (ข) redactText(s) สำหรับ log line. ผูกเป็น Fastify onSend hook ใน server.ts ครอบทุก JSON response (ครอบ /api/settings, /api/mcp, /api/permissions, effective view, memory, sessions, usage, activity ในครั้งเดียว) + ส่ง audit() writer ผ่าน redactText. Round-trip ของ editor (F-Set/F-MCP): แก้ PUT settings (governance.ts) + PUT mcp (extensions.ts) ให้ preserve ค่าบนดิสก์เมื่อ incoming value === "[REDACTED]" เพื่อไม่ให้ GET-แล้ว-PUT ทับ secret จริงด้วย sentinel. เพิ่ม test console/backend/test/redaction.test.ts: เขียน temp settings.json (env.ANTHROPIC_API_KEY=sk-ant-xxx) + .mcp.json (server env key) → GET ทั้งสอง endpoint ต้องไม่มีค่า secret ปรากฏ; PUT ด้วย sentinel ต้องคงค่าเดิมบนดิสก์; audit line ที่มี token ปลอมต้องถูก mask; negative case: ค่า non-secret ไม่ถูกแตะ. ปิดท้าย: อัปเดตหมายเหตุ §13.3 ใน PROGRESS.md จาก "documented decision" เป็น "conservative sweep + test แล้ว — การจูน policy เพิ่ม (custom patterns/allowlist) เป็น knob ให้มนุษย์ทบทวน" — ทั้งหมดทำได้โดยไม่ต้องรอ human input

## G18 [§14 (.ai/ tree) + §11.1] — 

**Why:** Confirmed. No goal.yaml/models.yaml/task-graph.json exists anywhere; .ai/policies/ and .ai/schemas/ hold only .gitkeep, so the §11.1-referenced gate-ladder.yaml/security-plane.yaml/fusion-profiles.yaml are also missing. Spec §14 lists these files as part of the .ai/ tree and PROGRESS.md's own Build Plan committed to that structure; §11.1 calls the goal contract a frozen versioned artifact at .ai/goal.yaml. Loops that already ran (supervised 3/3, semiauto) build goal contracts and tasks purely in

**Plan:** 1) Add yaml dep (ISC-licensed `yaml` package, commit lockfile) and a small loader/saver module, e.g. core/src/contract-store.ts: loadGoalContract(path) -> GoalContractLite-compatible object, saveTaskGraph(path, tasks), plus loadModelRegistry(path). 2) Serialize current in-code defaults into files: .ai/goal.yaml following the §11.1 template for the platform's bootstrap goal (quality_gates pointing at .ai/policies/gate-ladder.yaml etc.), .ai/policies/{gate-ladder,security-plane,fusion-profiles}.yaml mirroring the values already hardcoded in core/src/gates.ts, security-plane.ts, aal/src/fusion.ts, and .ai/models.yaml listing the registered adapters/models with role eligibility (anthropic verified; openai-compatible marked unverified per D-007). 3) Wire consumers: scripts/run-supervised-loop.mjs and run-semiauto-loop.mjs load the goal contract from .ai/goal.yaml (fall back to inline only if absent), write .ai/task-graph.json at plan time (tasks, deps, AC mapping, status) and update statuses on completion; aal router candidate list built from loadModelRegistry(.ai/models.yaml). 4) On loop start against a target project, write the goal.yaml into that project's .ai/ so the F-Proj loop-managed banner (console/backend/src/server.ts:152-153) actually triggers. 5) Tests: round-trip load/save; loop run produces .ai/task-graph.json whose tasks pass runPlanningGate; server /projects reports loopManaged=true after a loop writes goal.yaml. Keep CI green; commit on build/platform referencing §11.1/§14.

## G19 [§14 (.ai/lessons/ P4) + Phase 4 \] — .ai/ tree ระบุ "lessons/ (P4)" และ Phase 4 deliverable "lessons active"

**Why:** ยืนยันแล้ว: .ai/lessons/ ไม่มีอยู่จริง (ls .ai พบเฉพาะ agents/audit/calibration/console/evidence/policies/runs/schemas) และ core/src/lessons.ts เก็บ lesson ใน private in-memory Map เท่านั้น — ไม่เขียนไฟล์, constructor ไม่ replay event log, และ event LESSON_PROPOSED/LESSON_APPROVED มี payload แค่ { id } ไม่มี text ทำให้ replay ก็กู้ไม่ได้ — approved lessons หายเมื่อ process restart ขัดกับ spec §14 tree ที่ระบุ "lessons/ (P4)" และ Phase 4 deliverable "lessons active" ตรวจ docs/DECISIONS.md (D-000.

**Plan:** แก้ core/src/lessons.ts ให้ LessonStore persist ลง .ai/lessons/: (1) เพิ่ม parameter dir (path ไปยัง .ai/lessons/) ใน constructor — mkdir recursive แล้ว load ไฟล์ lesson-*.json ที่มีอยู่เข้า Map ตอนสร้าง (2) proposeFromHypothesis/approve เขียนไฟล์ .ai/lessons/<id>.json (fields: id, text, sourceTaskId, sourceHypothesis, approved) แบบ atomic (write tmp + rename) พร้อม append event ตามเดิม แต่ขยาย payload ของ LESSON_PROPOSED ให้มี text/sourceHypothesis ด้วยเพื่อให้ event log สมบูรณ์ (3) สร้าง .ai/lessons/.gitkeep (4) อัปเดต call sites ที่ new LessonStore (core/src/index.ts export + core/test/phase4.test.ts) ให้ส่ง dir — ใน test ใช้ temp dir (5) เพิ่ม test ใน core/test/phase4.test.ts: propose + approve แล้วสร้าง LessonStore instance ใหม่บน dir เดิม → injectable() ต้องคืน lesson ที่ approve ไว้ (survives restart) และ instance ใหม่ต้องไม่ inject lesson ที่ยังไม่ approve (6) รัน test + typecheck ให้เขียว ทั้งหมดไม่ต้องการ input จากมนุษย์ — human approval gate ยังเป็น approve() เหมือนเดิม ไม่แตะ governance semantics

## G20 [§14 (repo tree, root)] — โครงสร้าง repo ระบุ "└── src/ · test/{ai-generated, golden}/" ที่ root

**Why:** Spec §14 (unified-platform-spec.md:522) lists root-level `src/` in the platform repo tree, and PROGRESS.md:29 repeats `src/` in its own "(ข) Repo structure ที่จะสร้าง (§14)" plan with the checklist marking "Repo structure §14" done. Root `src/` does not exist, while its siblings `test/ai-generated/.gitkeep` and `test/golden/.gitkeep` were created as placeholders — so the builders treated §14 root entries as requiring placeholders and simply omitted `src/`. The omission is not on the pending-huma

**Plan:** Create the missing placeholder mirroring the existing test/ pattern: `mkdir /Users/king_developer/Desktop/Project/autonomous-engineering-platform/src && touch src/.gitkeep`, then commit on the working branch (not main, per workflow rules). No config/CI changes needed — nothing in tsconfig.base.json, vitest.config.ts, pnpm-workspace.yaml, or .github/ references root src/.

## G21 [§14 (scripts/)] — scripts/: "create-worktree.sh, rollback-worktree.sh, conformance.sh, calibrate.sh"

**Why:** ยืนยันตามหลักฐาน: spec §14 (unified-platform-spec.md:521) ระบุ scripts/ ต้องมี create-worktree.sh เป็นชื่อไฟล์ตรงตัว แต่ scripts/ จริงมีเพียง rollback-worktree.sh, check-vendor-neutral.sh, run-conformance.mjs (เทียบเท่า conformance.sh), run-supervised-loop.mjs (ผลิต .ai/calibration/ — เทียบเท่า calibrate.sh), run-semiauto-loop.mjs, run-fusion-measure.mjs — ไม่มี create-worktree.sh และไม่มี code path ใดสร้าง git worktree เลย (grep ทั้ง core/, aal/, adapters/, console/, scripts/, test พบเฉพาะ mkdt

**Plan:** เพิ่ม scripts/create-worktree.sh (POSIX sh, คู่กับ rollback-worktree.sh ที่มีอยู่): usage `create-worktree.sh <task-id> [base-ref]` → ตรวจว่าอยู่ใน git repo, สร้าง `git worktree add -b task/<task-id> worktrees/<task-id> ${2:-HEAD}` (ถ้า branch/dir มีอยู่แล้วให้ fail ด้วย exit code ชัดเจน — idempotent guard), echo path ที่สร้างเป็น stdout บรรทัดเดียวเพื่อให้ script อื่น capture ได้. เพิ่ม `worktrees/` ลง .gitignore. เพิ่ม test (เช่น core/test หรือ scripts test ใหม่ใน vitest) ที่: สร้าง temp git repo → รัน create-worktree.sh → assert dir + branch เกิดจริง → เขียนไฟล์สกปรกใน worktree → รัน rollback-worktree.sh → assert สะอาด (ปิด loop producer/consumer ที่ขาด). อัปเดต PROGRESS.md บรรทัดที่อ้าง rollback path ให้รวม create-worktree.sh. ไม่ต้อง refactor loop scripts (mkdtempSync ใช้ได้สำหรับ measure loops — ถ้าจะย้ายไปใช้ worktree จริงเป็นงานแยก). ทั้งหมดทำได้โดยไม่ต้องมีมนุษย์ ~30 บรรทัด script + 1 test file.

## G22 [§14 Phase 4] — 

**Why:** Phase 4 (spec line 549) lists "issue intake" as a deliverable and PROGRESS.md marks Phase 4 complete, but no intake mechanism exists: goals are hardcoded literals in scripts/run-supervised-loop.mjs and run-semiauto-loop.mjs; the only .ai/goal.yaml reference anywhere is an existsSync banner check in console/backend/src/server.ts:153; core/src/planning-gate.ts merely cites "Phase 4 issue intake" in a comment while validating an already-built task graph. Issue intake appears in neither PROGRESS.md'

**Plan:** File-based intake (vendor-neutral, no credentials or human input needed): (1) add core/src/issue-intake.ts exporting intakeIssue(issue) that validates a structured issue {id, title, objective, acceptanceCriteria[{id, description, verification}], scope?, constraints?} and converts it into a §11.1 goal-contract object; reject on missing id/title/ACs; if a goal contract already exists, emit a versioned amendment (goal.v2.yaml or version field bump) instead of overwriting the frozen contract; append an intake event to the event log (event-log.ts). (2) add scripts/intake-issue.mjs that reads issue files dropped in .ai/intake/*.json|yaml, calls intakeIssue, writes .ai/goal.yaml, then runs checkPlanningGate against the planned tasks before the loop may use the contract (blocking on failure, matching §11.2). (3) add a JSON schema .ai/schemas/issue.json to match §11.3 structured-I/O convention. (4) tests: valid issue → goal.yaml written + event logged; missing ACs → rejected; existing contract → amendment not overwrite; intake output feeds checkPlanningGate. (5) fix the misleading comment in core/src/planning-gate.ts (it is the planning gate, not intake) and update PROGRESS.md Phase 4 entry. Keep core/ vendor-name-free (INV-7); no tracker (GitHub etc.) integration — that would need credentials, note it as an optional adapter for later.

## G23 [§14 core (scheduler+lease) / Phase 0 scenario 8] — 

**Why:** Verified: claimLease/releaseLease exist only in core/src/event-log.ts (lines 73, 93) and their sole caller repo-wide is core/test/fault-injection.test.ts:116-119. Orchestrator.runTask (core/src/orchestrator.ts:22-78) starts the state machine and runs the propose/execute loop without ever acquiring the task lease, and both production entry points (scripts/run-supervised-loop.mjs:79, scripts/run-semiauto-loop.mjs:49) call runTask directly — so two concurrent loop runs on the same taskId can both w

**Plan:** Wire the existing lease into Orchestrator.runTask (core/src/orchestrator.ts): (1) at the top of runTask, generate an ownerId (e.g. `core-${process.pid}-${crypto.randomUUID()}`) and call this.log.claimLease(ctx.taskId, ownerId, ttlMs) with ttlMs derived from ctx.budget.maxWallclockMs (or a 60s TTL); if it returns false, append a LEASE_DENIED event and return { finalState: 'LEASE_HELD', iterations: 0, flaky: false } without any state transitions. (2) Re-call claimLease with the same ownerId at each loop iteration to renew (event-log.ts already allows same-owner re-claim). (3) Wrap the loop body so releaseLease(ctx.taskId, ownerId) runs in a finally block on every exit path (pass, escalate, throw). (4) Add a test in core/test: shared EventLog, first runTask in flight (stub agent that awaits a deferred) → second runTask on same taskId returns LEASE_HELD; after first completes, lease is freed and a new runTask proceeds; plus an expired-TTL takeover case. (5) Run the core test suite (pnpm test) to confirm fault-injection scenarios 1-9 still pass. No human input needed; no schema or spec changes required.

## G24 [§2 INV-10] — หลักฐานทุกชิ้น immutable + content-addressed ผูก commit hash + env hash + gate-config hash

**Why:** ยืนยัน gap จริง (status=partial ถูกต้อง) — ตรวจบน branch build/platform (HEAD 336a638) แล้ว: (1) ส่วนที่ทำแล้ว: gate-config hash เข้า evidence ทุกครั้ง (core/src/gates.ts:gateConfigHash → GateResult.gateConfigHash, ใช้ใน auditor.ts AUDIT_* events), resultHash/contentHash sha256 ใน core/src/executor.ts, event log append-only (INV-10 ครึ่งแรก). (2) ส่วนที่หายไป: ไม่มี commitHash / envHash ที่ไหนเลย — grep commitHash|commit_hash|rev-parse|envHash|env_hash ทั่ว core/src, aal/src, adapters/src, conso

**Plan:** เพิ่มไฟล์เดียว core/src/evidence.ts + แก้จุดต่อ ~4 ไฟล์: (1) evidence.ts: `envHash()` = sha256(JSON ของ {node: process.version, platform, arch, lockfileHash: sha256(pnpm-lock.yaml ถ้ามี)}) ตัด 16 ตัวเหมือน gateConfigHash; `commitHash(cwd)` = spawnSync('git',['rev-parse','HEAD'],{cwd}) สำเร็จ→hash, ไม่ใช่ git (เช่น audit tmp checkout)→'unversioned'; `writeEvidence(dir, record)` = canonical JSON → id=sha256(content), เขียน `<dir>/<id>.json` แบบ write-once (มีไฟล์อยู่แล้ว+เนื้อหาไม่ตรง→throw = immutable + content-addressed). (2) types.ts: เพิ่ม `commitHash: string; envHash: string` ใน GateResult. (3) gates.ts: runGate รับ `binding: {commitHash, envHash}` (หรือคำนวณเองจาก cwd ของ command แรก) แล้วใส่ลงทุก GateResult รวม not_enabled stub. (4) auditor.ts: ใส่ commitHash/envHash/evidenceId ลง payload ของ AUDIT_REPRODUCED/AUDIT_NON_REPRODUCIBLE + เรียก writeEvidence ไปที่ .ai/evidence/. (5) merge-queue.ts: หลัง land ใส่ commitHash ของ integrated state ลง MERGE_LANDED payload. (6) เทสต์ core/test/evidence.test.ts: assert (a) evidence record มีครบ 3 hash, (b) ไฟล์ชื่อ = sha256 ของเนื้อหา, (c) เขียนซ้ำเนื้อหาต่างกันที่ id เดิม → throw, (d) commitHash ใน git repo จริงตรง `git rev-parse HEAD`. ไม่ต้องการ input จากมนุษย์ — ไม่แตะ policy/threshold, core ยังคง vendor-neutral (git ไม่ใช่ vendor คำต้องห้าม INV-7)

## G25 [§2 INV-13] — ทุกจุดที่ spawn run แสดงสถานะโควตาโดยประมาณ

**Why:** INV-13 (spec line 77) requires every run-spawn point to show estimated quota status. Verified partial: Status page (web.ts:374 "Quota (estimate)" card), Scheduler guard banner (web.ts:848-849), and Usage HUD exist, but the F-Term spawn surfaces show nothing — pages.terminal (console/backend/src/web.ts:501-521) renders the Spawn button with no quota info, and grep for quota/usage/estimate in console/backend/src/terminal.ts and pty-manager.ts returns zero hits; there is no global quota HUD in the 

**Plan:** Reuse the existing /api/usage endpoint (already returns currentWindow.sessions, week.sessions, officialSource — consumed by pages.status). (1) In console/backend/src/web.ts pages.terminal (~line 502): fetch /api/usage alongside /api/term (Promise.all, .catch(() => null) so the page still renders if usage fails) and add one muted line inside the "New terminal" card, e.g. 'Quota (estimate): N sessions / 5h window · M / week — <officialSource>'. (2) In console/backend/src/terminal.ts /terminal launcher HTML: add a small header span and an inline fetch('/api/usage') that fills it with the same one-line estimate before/at spawn (fail-open if fetch errors). (3) Add a test asserting the terminal page markup/client code references the quota estimate (e.g. HTML contains 'Quota (estimate)' and '/api/usage'), following the style of the existing 85-test suite. Optional but cheap for full INV-13 coverage: have scripts/run-semiauto-loop.mjs / run-supervised-loop.mjs / run-fusion-measure.mjs print a one-line quota estimate from the same usage module before dispatching. No human input needed; no hardcoded caps (per §5.3), label stays "estimate" with official-source pointer.

## G26 [§2 INV-14] — Redaction ครอบทุก response/log ของ Console + path ของ credential files

**Why:** INV-14's Console redaction clause is normative and never-cut (spec lines 80, 488 'มี test', 566 ห้ามตัดเด็ดขาด). Implementation exists only by construction at two points (auth.ts env-name-only listing; server.ts audit log without payload bodies). governance.ts /api/memory and settings endpoints return raw file contents unmasked, search.ts returns raw session text, and no credential-file-path redaction exists anywhere. Not in docs/DEVIATIONS.md, not in docs/DECISIONS.md, not one of the 5 kickoff-

**Plan:** 1) Add console/backend/src/redact.ts exporting redactText(s: string): string that (a) masks secret values using the same pattern set as core/src/context-builder.ts SECRET_PATTERNS (export the array from core or copy the patterns; console→core import direction does not violate INV-7) replacing matches with [REDACTED], and (b) masks credential file paths with [REDACTED_PATH] via a small path pattern list: /\.claude\/\.credentials[^\s"']*/, /\.aws\/credentials/, /\.netrc/, /\.ssh\/id_[a-z0-9]+/, /\.env(\.[A-Za-z0-9_-]+)?\b/. 2) Wire it centrally in server.ts as a Fastify onSend hook that runs redactText over string/JSON response payloads for GET endpoints, and wrap the audit() writer so every audit line passes through redactText before being written. 3) Add console/backend/test/redaction.test.ts: (a) write a temp memory file containing a fake anthropic-style key and a fake ~/.claude/.credentials.json path, GET /api/memory returns content with [REDACTED]/[REDACTED_PATH] and never the raw values; (b) trigger an audit event whose fields contain a credential path and assert the log line is masked; (c) settings read with an env value matching a secret pattern is masked. 4) Update PROGRESS.md line-82 note to: baseline pattern-based sweep implemented per INV-14; pattern-set refinement/policy tuning remains a human item (add it explicitly to the pending-human list). Estimated scope: 1 new src file, 1 new test file, small edits to server.ts and PROGRESS.md.

## G27 [§4.1] — "Cross-platform: POSIX ผ่าน pty; Windows ผ่าน ConPTY (node-pty รองรับ) — verify พฤติกรรมจริงต่อเวอร์ชัน"

**Why:** Requirement is normative for built phases (§4.1 line 148 + F-Term row §8 line 331 marks "ConPTY บน Windows" as P0/Phase 1, and Phase 1 is marked done). Verification exists only for POSIX/macOS (spikes/RESULTS.md spike 2; all learnings are POSIX-specific). No Windows/ConPTY verification anywhere in repo, and it is absent from docs/DEVIATIONS.md (only DEV-001), docs/DECISIONS.md (D-000..D-008), PROGRESS.md pending-human list (5 items), and the Phase 4 intentional-cut list. Worse, console/backend/s

**Plan:** Actual Windows verification needs Windows hardware (human), so close as documented deferral + fail-closed guard, matching the existing executor.ts precedent: (1) Add DEV-002 to docs/DEVIATIONS.md: "§4.1 Windows/ConPTY — node-pty 1.1.0 supports ConPTY in principle but per-version behavior verification (required by §4.1) needs a Windows machine; build/verification environment is macOS-only. F-Term fails closed on win32 until verified." (2) Add item 6 to the pending-human list in PROGRESS.md: verify F-Term PTY parity on Windows/ConPTY with node-pty 1.1.0 (spawn, slash commands, resume, detach/reattach) per §4.1/§15.2. (3) In console/backend/src/pty-manager.ts spawn(), add a guard: if process.platform === 'win32', throw Error('F-Term on Windows/ConPTY is unverified — fail-closed until DEV-002 is resolved') before spawning; while there, make the full-shell fallback platform-aware only behind that guard (no behavior change on POSIX). (4) Add a unit test in console/backend/test/ that stubs process.platform to win32 and asserts spawn() throws (fail-closed), keeping CI green.

## G28 [§5.3] — 

**Why:** §5.3 (spec line 186) บังคับ 3 อย่างพร้อมกัน: ป้ายค่าประมาณ + deep-link แหล่งทางการ + calibration ให้ผู้ใช้กรอก % จริง (และ weekly reset ที่ผู้ใช้กรอก) — implementation ที่ console/backend/src/usage.ts อ้าง §5.3 เองและทำเฉพาะป้าย (line 73) กับ officialSource แบบ text ล้วน (line 74, ไม่ใช่ link) · server.ts มีแต่ GET /api/usage|/full|/alerts ไม่มี endpoint รับ input · pages.usage ใน web.ts (729-758) เป็น read-only ล้วน ไม่มี input field · grep 'calibrat' ใน .ts เจอแค่ placeholder string ./scripts/

**Plan:** เพิ่ม quota calibration ขั้นต่ำใน console: (1) usage.ts — เพิ่ม type Calibration { windowPct?: number; weekPct?: number; weeklyResetAt?: string; enteredAt: number } และฟังก์ชัน applyCalibration(est, cal, now) คืน utilization ที่ scale จาก % จริงที่กรอก (anchor: ตอนกรอก windowPct=X% ที่ sessions=N → factor = X/N ใช้คูณ count ปัจจุบันจน window/รอบ reset ถัดไปหมดอายุ แล้ว fallback เป็น estimate เดิม) และใช้ weeklyResetAt เป็น anchor นับ week แทน trailing 7 วัน (2) server.ts — GET/POST /api/usage/calibration (validate 0-100) persist เป็น JSON ในตำแหน่ง state เดิมของ console (ตามแพตเทิร์นไฟล์ state อื่นในโปรเจกต์) และให้ /api/usage + /api/usage/alerts รวมค่า calibrated พร้อม field บอกว่า calibrated เมื่อไร/หมดอายุหรือยัง (3) web.ts pages.usage — เพิ่ม card "Calibrate" มี input % (window/week) + datetime weekly reset + ปุ่ม Save เรียก POST, แสดง "calibrated จาก /usage เมื่อ <เวลา>" และเปลี่ยน officialSource ให้เป็น <a href="https://claude.ai/settings/usage"> จริง (ปิดข้อ deep-link ด้วย) ส่วน /usage ใน CLI คงเป็น text (4) vitest: applyCalibration scale ถูก, หมดอายุแล้ว fallback, validation ปฏิเสธค่านอกช่วง, weekly นับตาม reset anchor (5) บันทึกใน PROGRESS.md ว่าปิด gap §5.3 calibration

## G29 [§5.3] — "weekly caps สองตัว (รวมทุกโมเดล และเฉพาะ Sonnet) reset ตามเวลาประจำบัญชี" + "สะสม weekly ตามรอบ reset ที่ผู้ใช้กรอก"

**Why:** ยืนยันแล้วว่า gap จริง (status=partial ตามที่ auditor อ้าง) หลังค้นทั้ง repo: (1) สเปกเป็น normative สำหรับเฟสที่สร้างแล้ว — §5.3 บรรทัด 184/186 ("weekly caps สองตัว (รวมทุกโมเดล และเฉพาะ Sonnet) reset ตามเวลาประจำบัญชี" + "สะสม weekly ตามรอบ reset ที่ผู้ใช้กรอก") และตาราง §8 บรรทัด 341 กำหนด F-Usage = "Quota HUD (window 5h + weekly all/Sonnet + reset + Opus/Sonnet breakdown) + calibration" priority P0 phase 1(ย่อ)+2(เต็ม) — PROGRESS.md ติ๊ก Phase 1/2 ผ่านแล้ว จึงต้องมีของนี้. (2) implementation

**Plan:** แก้เล็กสุดใน console/backend (ไม่ต้องรอมนุษย์ — กลไก "ให้ผู้ใช้กรอก" คือสร้างช่องกรอก ไม่ใช่รอค่าจากผู้ใช้; default fallback = rolling 7d พร้อม label ชวนกรอก): 1) console/backend/src/usage.ts — เพิ่ม optional field `models?: string[]` ใน SessionStat และพารามิเตอร์ optional `weeklyReset?: { weekday: number; hour: number }` ใน estimateUsage; ถ้ามี anchor ให้คำนวณ weekStart = boundary ล่าสุดที่ <= now (คณิตล้วน ไม่แตะ timezone lib — ใช้ local time ของเครื่อง) ไม่มี anchor ให้คง rolling 7d; เปลี่ยน return `week` เป็น `{ allModels: {sessions,bytes}, sonnet: {sessions,bytes}, resetAt: number|null, mode: 'anchored'|'rolling' }` โดย sonnet นับ session ที่ models มี /sonnet/i (ห้าม hardcode เพดานตาม INV-13 — นับปริมาณอย่างเดียว). 2) console/backend/src/server.ts — เพิ่ม `GET/POST /api/usage/config` (validate weekday 0-6, hour 0-23) persist เป็น JSON ใน .ai/console/ (ที่เดียวกับ search.db); ใน /api/usage เติม models ให้ SessionStat จาก search index aggregates เมื่อ index มี แล้วส่ง config เข้า estimateUsage. 3) console/backend/src/web.ts หน้า usage — แยก KPI เป็น "Week (all models)" + "Week (Sonnet)" + แสดงเวลา reset ถัดไป (หรือป้าย "rolling 7d — enter your account reset time") + ฟอร์มเล็ก weekday/hour ยิง POST /api/usage/config; อัปเดตการ์ดย่อใน F-Status (web.ts:375) ให้ใช้ week.allModels. 4) console/backend/test/usage.test.ts — เพิ่มเทสต์: boundary ก่อน/หลังเวลา reset, นับเฉพาะ Sonnet ถูกตัวจาก models, fallback rolling เมื่อไม่มี anchor, และ validation ของ config endpoint (ใน test ของ server ถ้ามีอยู่แล้ว). รัน pnpm test + tsc ใน workspace console/backend ให้เขียวก่อน commit บน build/platform

## G30 [§5.3] — "Anthropic ไม่เผยแพร่เพดานตายตัวและปรับได้ — ห้าม hardcode เพดาน"

**Why:** Confirmed. console/backend/src/server.ts:122 and :208 both hardcode `sessions / 50` as the utilization denominator with no env/config/calibration override anywhere in the repo, and that value drives /api/usage/alerts, the extensions quota guard (deferUntilReset), and the loop-console automationGuard (yield-to-interactive, §10.2/INV-13). Spec §5.3 line 185 is explicit and normative for built phases ("ห้าม hardcode เพดาน") and line 186 additionally requires user calibration input, which does not e

**Plan:** Smallest agent-doable fix: (1) In buildServer (console/backend/src/server.ts), read a soft budget once: `const windowBudget = Math.max(1, Number(env.PLATFORM_USAGE_WINDOW_BUDGET ?? 50));` (env already injectable via ServerDeps.env), with a comment stating it is an operator-calibrated soft operating budget per §5.3, not an official Anthropic cap. (2) Replace both `/ 50` literals with `/ windowBudget` — dedupe by having the /api/usage/alerts handler call the existing `utilization()` helper instead of recomputing (removes line 208 duplication). (3) Surface the value: include `budget: windowBudget` (and the estimate label) in the /api/usage/alerts response so the UI shows which denominator was used. (4) Tests: keep existing default-behavior tests (60/50 > 0.85 still passes); add one test in console/backend/test that builds the server with `env: { PLATFORM_USAGE_WINDOW_BUDGET: '10' }` and asserts utilization/guard reflects the override (e.g. 9 sessions -> 0.9 -> guard blocks). (5) Optionally append one line to docs/DECISIONS.md noting the default 50 is a reversible calibration default per §0.7. No human input needed; runtime calibration UI (user entering real % from /usage) can remain future work but the hardcode prohibition is satisfied.

## G31 [§6.1] — 

**Why:** CONFIRMED. Spec §6.1 (line 210) + §10.1 + Phase 2 roadmap make the allowlist exception path (package_install → pinned registry + --ignore-scripts) normative for Phase 2, and PROGRESS.md lines 60-63 mark Phase 2 "canary/dep-policy" as DONE (2026-07-05). But core/src/policy.ts:53-57 still denies every network != 'none' with the stale comment "arrive with dependency policy (Phase 2); until then everything is deny"; the NetworkPolicy type (core/src/types.ts:5) supports `allowlist:<n>` yet no value c

**Plan:** Smallest agent-doable fix, all in core/ (vendor-free, no human input): (1) core/src/policy.ts — in the RUN_COMMAND case, replace the blanket deny with: if network === 'allowlist:package_install' return allow('dependency-policy'); any other allowlist:<n> stays deny('egress-default-deny', 'no policy for allowlist <n>'); remove the stale "not enabled in this phase" comment. (2) core/src/executor.ts — add an optional depPolicy?: DependencyPolicy constructor param (default { allowedRegistries: ['https://registry.npmjs.org/'], installersAllowed: ['pnpm','npm'] }); in apply() RUN_COMMAND, when action.network === 'allowlist:package_install', call checkPackageInstall(this.log, ctx.taskId, action, this.depPolicy): if !allowed → return rejected with policy 'dependency-policy' and append EGRESS_DENIED; if allowed → spawn with decision.rewrittenArgs (registry pinned + --ignore-scripts forced) WITHOUT the egress-deny wrapper for this sanctioned action only (darwin/linux; keep the existing fail-closed null-wrapper rejection for network:'none' paths), and rely on the PACKAGE_INSTALL_SANCTIONED event checkPackageInstall already appends. (3) Tests in core/test/phase2.test.ts (or a new executor test): (a) executor rejects { network: 'allowlist:package_install', cmd: 'curl' } with policy 'dependency-policy'; (b) executor rejects { network: 'allowlist:other' } with 'egress-default-deny'; (c) executor accepts { cmd: 'pnpm', args: ['add','x','--registry=https://evil.example'] } and the ACTION_INTENT/spawn args contain '--ignore-scripts' and 'registry.npmjs.org' but not 'evil.example' (use a spawn seam or assert via the logged PACKAGE_INSTALL_SANCTIONED event + a hooks-based dry-run to avoid real network in CI); (d) existing fault-injection test 3 (network escape blocked) must still pass unchanged. Then run pnpm test + lint and update the Phase 2 checklist line in PROGRESS.md to note the wiring commit.

## G32 [§6.1] — Action DSL รวม `APPLY_PATCH { diffRef }` เป็น action type ที่ executor รองรับ

**Why:** core/src/executor.ts:122-124 rejects APPLY_PATCH unconditionally ('not enabled in this phase') with a comment promising it 'lands with Phase 1 agents', but PROGRESS.md declares Phases 0-4 complete. Repo-wide search confirms no implementation exists anywhere: types.ts:9 declares the variant (as `diff`, not spec's `diffRef`), policy.ts:32 has a stub branch that skips golden/role checks (rel=''), adapters only emit WRITE_FILE, and no test covers APPLY_PATCH. The cut is undocumented: docs/DEVIATIONS

**Plan:** Smallest close, all agent-doable: (1) core/src/executor.ts — implement `case 'APPLY_PATCH'`: write action.diff to a temp file, spawnSync `git apply --check` then `git apply` with cwd=ctx.worktree (same minimal env as RUN_COMMAND, no network); if `git apply --reverse --check` succeeds treat as already-applied and return applied (idempotency per §6.1 actionId semantics); on failure return structured reject. resultHash = sha256(diff). (2) core/src/policy.ts — in the APPLY_PATCH branch, parse target paths from diff headers (`--- a/`, `+++ b/`, `rename to`) and run each path through the existing confine() + test/golden read-only + role WRITE_PREFIXES checks used for WRITE_FILE; unparseable diff → deny. (3) adapters/src/anthropic.ts (~lines 30, 132) and openai-compatible.ts — add APPLY_PATCH to the action grammar string and the parse switch ({type:'APPLY_PATCH', diff}) so agents can emit it. (4) New core/test/apply-patch.test.ts using core/test/helpers.ts: valid patch applies as implementer; patch touching test/golden denied; planner denied; path-escape (../) denied; re-apply is idempotent. (5) Record the `diff: string` vs spec `diffRef` (blob ref) shape choice as a DEV-002 entry in docs/DEVIATIONS.md, and update the PROGRESS.md note. Verify: pnpm test green (72 existing + new).

## G33 [§6.2] — export `events.jsonl` เพื่อ audit; `state.json` = projection rebuild ได้ (สอดคล้อง INV-10)

**Why:** Confirmed missing. core/src/event-log.ts has only append/eventsFor/all/lease — no JSONL export method, no script/endpoint anywhere (core, aal, adapters, console, scripts, spikes) that exports the platform event log as events.jsonl, and no state.json projection writer/rebuilder. The JSONL hits in console (sessions-ops.ts export) are F-Sess Claude Code transcript export, and auditor.ts/executor.recover() are artifact-rebuild/crash-recovery — all different features. Not a documented cut: DEVIATIONS

**Plan:** Smallest close, all in core (vendor-free, no human input needed): (1) core/src/event-log.ts — add exportJsonl(outPath: string): number that iterates the events table ordered by seq and writes one JSON.stringify(event) per line to outPath, returning the line count. (2) core/src/event-log.ts (or a new tiny core/src/projection.ts) — add rebuildState(outPath: string) that replays all() into a projection { tasks: { [taskId]: { state, lastSeq, lastTs } } } using the existing STATE_CHANGED/transition event types from state-machine.ts, and writes it as state.json; deterministic so delete-and-rebuild yields identical JSON. (3) scripts/export-events.mjs — thin CLI: node scripts/export-events.mjs <db> <outDir> calls both, writing <outDir>/events.jsonl and <outDir>/state.json. (4) Tests in core (vitest, alongside existing event-log tests): append N events -> exportJsonl writes N lines that parse back deep-equal to all(); rebuildState -> unlink state.json -> rebuildState again -> byte-identical content (proves INV-10 rebuildable). (5) Note the closure in PROGRESS.md spec-audit section. Estimated ~80 lines total including tests.

## G34 [§6.2] — Lease ต่อ task: `LEASE_CLAIMED {taskId, ownerId, leaseUntil}` + heartbeat + TTL → single-writer

**Why:** ยืนยันแล้ว: claimLease/releaseLease (atomic CAS + TTL) มีจริงใน core/src/event-log.ts:73-98 และมี fault-injection test 8 แต่ grep ทั้ง repo (core, aal, adapters, console, scripts, spikes, test) พบผู้เรียกเฉพาะ core/test/fault-injection.test.ts เท่านั้น — Orchestrator.runTask (core/src/orchestrator.ts:22-78) และ loop จริงทั้งสอง (scripts/run-supervised-loop.mjs:79, run-semiauto-loop.mjs:49) เรียก runTask โดยไม่ claim lease และไม่มี heartbeat/renew ทั้ง API และ caller ที่ไหนเลย ไม่ใช่ documented c

**Plan:** 1) core/src/event-log.ts: เพิ่ม renewLease(taskId, ownerId, ttlMs): boolean — UPDATE leases SET lease_until WHERE task_id=? AND owner_id=? (return changes>0) เพื่อให้ heartbeat ไม่ spam LEASE_CLAIMED events. 2) core/src/orchestrator.ts: ใน runTask ก่อนสร้าง StateMachine เรียก this.log.claimLease(ctx.taskId, ownerId, LEASE_TTL_MS) โดย ownerId = `core-${process.pid}` (หรือรับผ่าน constructor option); ถ้า claim ไม่ได้ ให้ append event LEASE_REJECTED แล้ว return { finalState: 'BLOCKED', iterations: 0, flaky: false } (ไม่ throw); ตั้ง setInterval heartbeat = renewLease ทุก TTL/3 (unref()); ครอบ loop เดิมด้วย try/finally → finally: clearInterval + releaseLease. 3) Test ใหม่ใน core/test: (a) runTask สอง instance บน taskId เดียวกัน ตัวที่สองได้ BLOCKED ขณะ lease ถูกถือ, (b) renewLease ขยาย lease_until เกิน TTL เดิม และคืน false เมื่อ owner ไม่ตรง, (c) หลัง runTask จบ lease ถูก release แล้ว claim ใหม่ได้. 4) pnpm -C core build (scripts import จาก core/dist) + รัน vitest ทั้งชุดให้ผ่าน. ไฟล์ที่แตะ: core/src/event-log.ts, core/src/orchestrator.ts, core/test/*.test.ts — ไม่ต้องแก้ scripts เพราะ lease ถูก enforce ที่ Orchestrator ชั้นเดียวครอบทุก caller

## G35 [§6.4] — T0 fast: รันเมื่อ 'ทุก iteration' (lint, typecheck, targeted tests)

**Why:** Confirmed. Spec §6.4 (unified-platform-spec.md:234) marks T0 as Phase 0 with trigger "ทุก iteration", but core/src/orchestrator.ts:51 `continue`s on any iteration where the agent does not claim GREEN/READY_FOR_VERIFICATION, so no gate runs at all on those iterations; T0 (orchestrator.ts:54) executes only inside the claim branch — the trigger the spec assigns to T1. Repo-wide search shows no other T0 execution path (grep for T0/runGate hits only gates.ts/orchestrator.ts/types.ts; executor.ts runs

**Plan:** In core/src/orchestrator.ts, run T0 on every iteration right after executing the proposal's actions (after line 42): `const t0 = runGate('T0', this.gates);` — log a GATES_RUN event with `{ t0, iteration }` and gateConfigHash (INV-10), OR-accumulate `flaky` from t0. On non-claiming iterations, if T0 fails, surface it to the agent by appending a synthetic failure entry to `feedback` (or a dedicated field on the next propose() input) and `continue`. In the claim branch, reuse this iteration's t0 result instead of re-running (replace line 54), and run T1 only when t0 passed, as today. Add a fault-injection test in core/test/fault-injection.test.ts: use a GateConfig whose t0 command appends to a counter file (or a spy via a scriptable command), drive a mock agent that claims nothing for iterations 1-2 and GREEN on iteration 3, then assert the T0 command ran 3 times and GATES_RUN events exist for every iteration; add a second case asserting a failing T0 on a non-claiming iteration is reflected in the next propose() feedback. Run pnpm test to keep the existing 85 tests green. No human input required; no DEVIATIONS entry needed once conforming.

## G36 [§6.5] — 

**Why:** Spec §6.5 marks Convention gate as Phase 0 hard and §6.4 puts it inside T1 (Phase 0); Phase 0-4 are marked done in PROGRESS.md, so it is normative. No convention/forbidden-pattern checker exists anywhere in the repo: core/src/gates.ts is a generic command runner, and every real GateConfig t1 (scripts/run-supervised-loop.mjs:56, scripts/run-semiauto-loop.mjs:32, core/test/helpers.ts:43) contains only visible tests + golden tests. All grep hits are false positives ('forbidden-auto' L4 merge action

**Plan:** 1) Add core/src/convention-gate.ts: export checkConventions(worktree: string, rulesPath: string) that loads a JSON rules file ([{ name, pattern (regex), fileGlob, message }]), scans matching files under the worktree (skip node_modules/.git), and returns { ok, violations[] }; plus a tiny CLI entry scripts/check-conventions.mjs that runs it and exits 1 with violation list on stdout. 2) Ship a starter rules file .ai/conventions.json with a few enforceable forbidden patterns (e.g. forbid 'console.log' in src/, forbid 'it.only|describe.only|.skip(' in tests — aligns with the CI-gate rule already in house style, forbid hardcoded 'sk-'/'API_KEY=' literals). 3) Wire into T1: add GateCommand { name: 'convention-gate', cmd: 'node', args: [scriptPath, rulesPath], cwd: wt } to t1 in scripts/run-supervised-loop.mjs, scripts/run-semiauto-loop.mjs, and core/test/helpers.ts fakeGates (INV-10 gate-config hash already covers it automatically since hash is over the whole GateConfig). 4) Tests: unit test for checkConventions (violation found / clean pass / bad regex fails closed) + one fault-injection-style test asserting runGate('T1', ...) fails when a worktree file contains a forbidden pattern and passes when clean. 5) Update PROGRESS.md one line noting convention gate now in T1. No human input needed; rules file is extensible later by humans via meta-governance.

## G37 [§6.5] — Golden tests: เชื่อได้เท่าที่ golden ครอบ — วัด golden coverage คู่เสมอ

**Why:** §6.5 กำหนด (Phase 0) ว่าต้อง "วัด golden coverage คู่เสมอ" กับทุกการ claim จาก golden tests แต่ตรวจทั่ว repo แล้วไม่มีกลไกวัดจริง: core/src/golden.ts มีแค่ manifest-hash integrity (string 'golden coverage = 0' ที่บรรทัด 38 เป็นแค่ข้อความกรณี pre-freeze), core/src/gates.ts T1 ตรวจแค่ hash ไม่ใส่ coverage ลง evidence, planning-gate.ts วัด plan-task→AC ไม่ใช่ golden→AC, และ .ai/calibration/bootstrap-haiku-2026-07-04.json รายงาน heldOutPassed 3/3 โดยไม่มี coverage field คู่ (ขัด §12 ด้วย) ทั้งนี้ไม่

**Plan:** 1) เพิ่ม `measureGoldenCoverage(goldenDir, acIds: string[])` ใน core/src/golden.ts: เดิน tree ใต้ test/golden/ (ข้าม _MANIFEST.sha256) แล้ว match AC id กับ path ตาม convention `test/golden/<AC-ID>/**` (หรือชื่อไฟล์ขึ้นต้นด้วย AC id) คืน { goldenFileCount, coveredAcIds, uncoveredAcIds, coverage: covered/total (0 เมื่อ total=0 หรือ pre-freeze) } — export ผ่าน core/src/index.ts 2) ต่อเข้า core/src/gates.ts: เมื่อ T1/T2 ตรวจ golden ให้แนบผล coverage ลงใน detail/evidence ของ GateResult เสมอ (ไม่ใช่แค่ pass/fail) 3) แก้ scripts/run-supervised-loop.mjs และ run-semiauto-loop.mjs ให้เขียน field `goldenCoverage` ลงไฟล์ผล calibration (.ai/calibration/*.json) คู่กับ heldOutPassed ตาม §12 4) unit tests ใน core: dir ว่าง/pre-freeze → coverage 0 + uncovered ครบ; dir ที่มีไฟล์ match บาง AC → covered/uncovered ถูกต้อง; ตรวจว่า gate detail มี coverage ปรากฏ — รัน CI เดิม (85 tests) ต้องเขียวไม่แตะ behavior เดิม

## G38 [§6.6] — Budget (iterations/costUnits/wallclock) = backstop ที่ทำงานเสมอ

**Why:** ยืนยัน gap จริง: spec §6.6 (unified-platform-spec.md:264) กำหนด budget backstop ครบ 3 มิติ (iterations/costUnits/wallclock) "ทำงานเสมอ" และ orchestrator.ts:2 อ้างเองว่า implement §6.6 แต่ orchestrator.ts:32 ตรวจเฉพาะ maxIterations + maxWallclockMs; Budget.maxCostUnits (core/src/types.ts:55) ไม่มีโค้ด enforcement ใดอ่าน (พบเฉพาะ test helper + scripts ที่ set ค่าแล้วไม่ถูกใช้); adapter คำนวณ usage.costUnits (anthropic.ts:167, openai-compatible.ts:86) และ protocol มี AgentResponse.usage.costUnits แ

**Plan:** 1) core/src/types.ts: เพิ่ม `usage?: { costUnits: number }` ใน interface Proposal. 2) core/src/orchestrator.ts: เพิ่ม `let costUnitsUsed = 0` ก่อน loop; หลัง `await agent.propose(...)` สะสม `costUnitsUsed += proposal.usage?.costUnits ?? 0`; ขยายเงื่อนไข budget check บรรทัด 32 เป็น `|| costUnitsUsed > ctx.budget.maxCostUnits` และใส่ costUnitsUsed ใน payload ของ BUDGET_EXCEEDED (path ESCALATED เดิมใช้ร่วมได้). 3) aal/src/bridge.ts: ใน adapterAgentPort ใส่ `proposal.usage = { costUnits: res.usage.costUnits }` จาก AgentResponse. 4) Test: core/test/fault-injection.test.ts เพิ่ม case fake AgentPort ที่คืน proposal พร้อม usage.costUnits สูงจนสะสมเกิน maxCostUnits ก่อนถึง maxIterations/maxWallclockMs → expect event BUDGET_EXCEEDED + finalState ESCALATED; และ test ฝั่ง aal ว่า bridge ส่ง usage.costUnits ผ่านเข้า Proposal. รัน pnpm test ยืนยันเขียวทั้ง 72+ tests เดิม.

## G39 [§6.6] — failure fingerprint = advisory เร่งสลับกลยุทธ์/โมเดล

**Why:** ยืนยัน gap จริง. หลักฐาน: (1) grep 'fingerprint' ทั้ง repo (ยกเว้น node_modules) พบเฉพาะใน unified-platform-spec.md:264 เอง — ไม่มีใน core/, aal/, adapters/, console/, scripts/, test/, docs/ (2) core/src/orchestrator.ts implement ครึ่งแรกของบรรทัดสเปกนี้แล้ว (comment บรรทัด 2 อ้าง "budget backstop always on (§6.6)" + BUDGET_EXCEEDED ที่บรรทัด 32-38) แสดงว่า §6.6 บรรทัดนี้ถูกถือเป็น normative และอยู่ในขอบเขต build — แต่ครึ่งหลัง (failure fingerprint advisory) ไม่มี (3) core/src/repair.ts บันทึก r

**Plan:** สร้าง core/src/failure-fingerprint.ts (~40 บรรทัด, vendor-free ตาม INV-7): (1) computeFingerprint(gateResult): normalize ข้อความ fail (ตัด path/เลขบรรทัด/timestamp, เก็บชื่อ gate tier + ชื่อ test ที่ fail + error first-line) → sha256 สั้น (2) class FailureTracker: บันทึก fingerprint ต่อ task, เมื่อ fingerprint เดิมซ้ำ >= 2 ครั้งติด → คืน advisory (3) hook ใน core/src/orchestrator.ts จุดที่ gate fail แล้วสร้าง feedback: append event `STRATEGY_SWITCH_ADVISED` (advisory-only, payload: fingerprint, count, suggestion: 'switch strategy/model per §6.6/§7.4') ลง EventLog และแนบ advisory ลง feedback ที่ส่งกลับ agent — ไม่เปลี่ยน control flow (budget ยังเป็น backstop เดียว) (4) test ใน core/test: fail เดิมซ้ำ 2 ครั้ง → มี event STRATEGY_SWITCH_ADVISED; fail ต่างกัน → ไม่มี; fingerprint คงที่แม้ path/เลขบรรทัดต่าง. ไม่ต้องมี human input เพราะเป็น advisory event + feedback เท่านั้น ไม่แตะ gate/merge policy (INV-16 ไม่เกี่ยว)

## G40 [§7.1] — 

**Why:** Confirmed. Spec unified-platform-spec.md:273 (§7.1, Phase 1 — already built per PROGRESS.md) normatively lists toolDefs in AgentRequest. aal/src/protocol.ts:18-26 has every field except toolDefs; grep across the whole repo finds "toolDefs" only in the spec file. Adapters hardcode the REQUEST_TOOL/action DSL in a static SYSTEM_PROMPT (adapters/src/anthropic.ts:24-32, adapters/src/openai-compatible.ts:13) and aal/src/bridge.ts:35-50 builds the invoke() envelope with no tool-definition field. Not c

**Plan:** 1) aal/src/protocol.ts: add `export interface ToolDef { name: string; description: string; argsSchema?: Record<string, unknown> }` and add `toolDefs: ToolDef[]` to AgentRequest (comment: catalog of tools core is willing to execute via REQUEST_TOOL). 2) aal/src/bridge.ts: extend BridgeTask with optional `toolDefs?: ToolDef[]` and pass `toolDefs: task.toolDefs ?? []` in adapter.invoke(). 3) adapters/src/anthropic.ts, openai-compatible.ts, _template.ts: when req.toolDefs.length > 0, append a marked-data section to the prompt (e.g. "AVAILABLE TOOLS (propose via REQUEST_TOOL, never execute):" + JSON.stringify(req.toolDefs)) — the DSL grammar itself stays adapter-side per INV-8 wire-format-translation. 4) aal/src/conformance.ts: add `toolDefs: []` to baseRequest and have P5 pass `[{ name: 'fusion.deliberate', description: '...' }]` so the tool-request probe exercises the new field. 5) Add a unit test asserting the rendered prompt contains a supplied toolDef name (mock fetch/query in the adapter test), run pnpm vitest, rebuild dist if dist/ is committed. 6) No DEVIATIONS entry needed once implemented; alternatively if implementation is deferred, record it as a DEV entry — but the code fix above is small enough to just do.

## G41 [§7.2] — structured output fallback = schema-in-prompt + validate + bounded repair loop

**Why:** Confirmed partial. Spec §7.2 (unified-platform-spec.md:276-281) mandates a CORE-owned fallback "schema-in-prompt + validate + bounded repair loop". Actual: adapters/src/anthropic.ts:120-126 does schema-in-prompt + one repair round, but "validate" is only JSON.parse + typeof parsed.result === 'object' — structuredResult is never validated against req.outputSchema (e.g. bridge.ts declares OUTPUT_SCHEMA required:['claim'] that nothing enforces; grep finds no ajv/json-schema/validator in aal/, core/

**Plan:** 1) Add aal/src/schema-validate.ts: dependency-free JSON-Schema subset validator export function validateSchema(value: unknown, schema: Record<string, unknown>, path='$'): string[] supporting type/properties/required/enum/items/additionalProperties — covers every schema currently used (bridge OUTPUT_SCHEMA, conformance probes); ~60 lines, avoids new dependency (per user dependency rules). 2) Add aal/src/structured-fallback.ts: export async function invokeWithRepair(runOnce: (repairNote?: string) => Promise<string>, outputSchema: Record<string, unknown>, extractJson: (t: string) => Record<string, unknown> | null, maxRepairRounds = 1): parse → validateSchema(parsed.result, outputSchema) → if violations, re-run once with repair note listing the exact violations → re-validate → throw Error listing violations if still failing. Export both from aal/src/index.ts. 3) Refactor adapters/src/anthropic.ts to call invokeWithRepair (replacing the inline parsed.result check at lines 119-126, keeping its existing run(repairNote) closure and extractJson). 4) Refactor adapters/src/openai-compatible.ts: move the fetch into a run(repairNote?) closure (repair note appended as extra system/user message) and wrap with invokeWithRepair so it gains the bounded repair round. 5) Tests in aal/test/structured-fallback.test.ts: validator unit cases (missing required, wrong type, enum violation, pass); fake runOnce that returns schema-violating JSON first then valid JSON → exactly 2 calls and valid result; always-invalid → throws with violation list; plus existing runConformance against a stub adapter still 8/8. 6) pnpm vitest green, commit on build/platform branch per D-005 (no push to main).

## G42 [§7.2] — context เล็ก → core fallback: context selection เข้มขึ้น → เกินอีกให้ลด scope task

**Why:** Confirmed. core/src/context-builder.ts:50,72-73 enforces only a per-file cap (maxBytesPerFile 64k truncate); there is no total-bundle byte/token budget. CapabilityManifest.contextWindowTokens (aal/src/protocol.ts:53) is declared and set by all three adapters but never read by core, router (aal/src/router.ts filters only structuredOutput/toolCalling), or bridge (aal/src/bridge.ts passes the bundle through unchecked) — the sole read is a test fixture in aal/test/breaker-router.test.ts:12. No stric

**Plan:** Smallest agent-doable implementation, all deterministic (no model calls needed for the mechanism itself): (1) core/src/context-builder.ts — add optional `maxTotalBytes?: number` to ContextBuildInput; after assembling pieces, if sum of piece bytes > maxTotalBytes, run one stricter pass: progressively halve the effective per-file cap (re-truncate file pieces) until the bundle fits or a floor (e.g. 4k/file) is reached; record `degraded: true` plus dropped/re-truncated entries in the manifest. If it still cannot fit (docs+canary alone exceed budget), return `overflow: true` in BuiltContext instead of silently truncating. (2) Wire the budget from the manifest: at the call site that already knows the routed adapter (aal/src/bridge.ts adapterAgentPort, or the loop scripts scripts/run-*.mjs that call buildContext then invoke), compute maxTotalBytes = adapter.manifest().contextWindowTokens * 4 * 0.6 (bytes-per-token heuristic, reserve for schema/feedback/output) and pass it into buildContext. (3) Scope reduction on second overflow: when buildContext returns overflow=true, the orchestrator/loop must not invoke the adapter — emit a BLOCKED(context_overflow) event via the existing EventLog/state machine with a note listing which seeds to split (e.g. per acceptance criterion), mirroring the existing BLOCKED(no_capacity) pattern in aal/src/router.ts so INV-5 (no thrash) is preserved. (4) Tests: core/test/context-builder.test.ts — bundle over maxTotalBytes triggers stricter re-truncation with degraded manifest and is deterministic (same manifestRef across two runs); bundle that cannot fit returns overflow=true; aal-side test — adapter manifest with tiny contextWindowTokens causes the bridge/loop path to produce BLOCKED(context_overflow) instead of invoking the adapter.

## G43 [§7.3] — Conformance P1–P8 (ประตูเสียบโมเดล + drift canary)

**Why:** Confirmed: P1-P8 exist only as a one-shot registration gate (aal/src/conformance.ts, sole caller scripts/run-conformance.mjs, 8/8 recorded in PROGRESS.md Phase 1). No drift-canary mechanism exists: adapterMeta.modelVersion is emitted by adapters/src/anthropic.ts:174 and adapters/src/openai-compatible.ts:88 but never consumed; nothing re-runs probes periodically or on modelVersion change; the only 'drift' code is worktree drift in core/src/executor.ts:130,159 (rollback/crash-recovery, unrelated t

**Plan:** Smallest agent-doable implementation (no live-model calls needed in CI, spec §16 allows manual cadence): (1) Baseline record — extend scripts/run-conformance.mjs to persist .ai/calibration/conformance-{adapterId}.json with {adapterId, modelVersion, probe results, timestamp} on a passing run. (2) New aal/src/drift-canary.ts with checkDrift(baseline, adapterMeta): returns 'drift-suspect' when live adapterMeta.modelVersion differs from the baseline's; wire it into the loop scripts (run-supervised-loop.mjs / run-semiauto-loop.mjs) at adapter-response time so a version change marks the adapter drift-suspect, emits an event to the event log, opens the adapter's breaker (aal/src/breaker.ts) until P1-P8 are re-run, and freezes shadow-routing updates (aal/src/shadow-routing.ts) per spec line 414. (3) Add --canary flag to run-conformance.mjs that re-runs the cheap subset (P1, P7), compares pass/fail to baseline, and updates the record — the manual re-run path §16 permits. (4) Stub-adapter unit tests: modelVersion changed → drift flagged + breaker open + shadow frozen; unchanged → no-op; re-run 8/8 → cleared. (5) One-line notes in PROGRESS.md and docs/DECISIONS.md stating drift canary = on-version-change trigger + manual --canary cadence per §16.

## G44 [§7.4] — 

**Why:** Confirmed. aal/src/router.ts implements only capability match (structuredOutput/toolCalling) + breaker/quota admit with first-eligible-wins. Missing and not documented anywhere as cut or pending-human: (1) injection-aware step — P7 result from aal/src/conformance.ts is never persisted per adapter nor consumed by route(); core/src/security-plane.ts only emits an advisory note, while spec makes susceptibility routing normative in three places (§7.4 pipeline, §7.3 "P7 → susceptibility routing", rin

**Plan:** Smallest agent-doable close, all testable with fake adapters (no credentials needed): (1) aal/src/protocol.ts — add optional CapabilityManifest fields reasoning?, largeContext?, codeProposal?, relativeCostRank?; set them in adapters/src/anthropic.ts and openai-compatible.ts. (2) aal/src/router.ts — extend RouteEntry with susceptibilityHigh?: boolean (populated from stored P7 conformance result; add a small registry record write in scripts/run-conformance.mjs that persists per-adapter P7 pass/fail, e.g. .ai/adapters/<id>.json, and read it when building entries); extend RoleNeeds with reasoning/largeContext/codeProposal; extend route(entries, needs, opts?: { lowTrustContext?: boolean; shadow?: ShadowRouter }) to: filter capabilities (largeContext via contextWindowTokens threshold) → filter breaker.admit() → if lowTrustContext, drop susceptibilityHigh entries (fail-closed to BLOCKED(no_capacity) if none) → sort by relativeCostRank asc → return { ok, entry, eligible: RouteEntry[] } (ordered list) and call shadow.observe(entry.adapter.manifest().adapterId) when provided. (3) switch_to_next_eligible — in aal/src/bridge.ts or the loop scripts, build entries from registered adapters and call route(); track consecutive failed iterations (feedback loop already counts iterations) and after N failures re-route excluding the failing adapterId, taking the next item of eligible; with one adapter this degrades to current behavior. (4) aal/test — add cases: low-trust context skips high-susceptibility adapter; cheaper capable adapter wins cost ordering; next-eligible returned after excluding failed adapter; all-excluded → no_capacity; shadow.observe called with live choice. (5) Update PROGRESS.md Phase 2/4 notes to reflect the completed §7.4 pipeline.

## G45 [§7.4] — Test Designer/Property … ต้องต่าง lineage กับ implementer

**Why:** Confirmed missing. CapabilityManifest (aal/src/protocol.ts:49) has no lineage field; router (aal/src/router.ts) filters only on structuredOutput/toolCalling and has no lineage-diversity constraint; the only 'lineage' occurrence in code is a reason string in core/src/merge-policy.ts:31. The test_designer role is never dispatched (aal/src/bridge.ts:37 hardcodes 'implementer'). Spec §7.4 makes the constraint mandatory (ต้อง) and §6.5 assigns Property/Adversarial (ต่าง lineage) to Phase 3, which PRO

**Plan:** 1) Add `lineage: string` to CapabilityManifest in aal/src/protocol.ts (vendor-neutral value declared by each adapter: adapters/src/anthropic.ts -> 'anthropic', openai-compatible.ts -> 'openai-compat', _template.ts -> placeholder). 2) Extend RoleNeeds in aal/src/router.ts with optional `distinctFromLineage?: string`; in route(), filter out adapters whose manifest().lineage equals it; if that empties the capable set, return {ok:false, reason:'no_distinct_lineage', detail:...} (new reason variant) so callers degrade explicitly instead of silently reusing the implementer lineage. 3) Add a small helper (e.g. routeTestDesigner(entries, implementerLineage) in aal/src/router.ts or bridge) that routes test_designer requests with distinctFromLineage set to the implementer's routed lineage — this is the enforcement point §7.4 requires. 4) Vitest coverage in aal tests: (a) two adapters with different lineages -> test_designer never gets the implementer's lineage; (b) single-lineage registry -> returns no_distinct_lineage (fail-closed, not same-lineage fallback); (c) all shipped adapters declare non-empty lineage. 5) Docs: append one paragraph to D-007 (or a DEV entry) stating that with only one verified lineage the constraint resolves to no_distinct_lineage and test-designer work is annotated/blocked until a second lineage passes conformance; note the closure in PROGRESS.md. No credentials or human input needed; all changes are in aal/, adapters/, docs/, tests.

## G46 [§7.5] — 

**Why:** Confirmed partial. aal/src/fusion.ts (52 lines, single export fusePanel) implements only the code-diff slice: PANEL (N sequential adapter invokes with distinct requestIds) -> EVIDENCE (caller-supplied gate runner) -> evidence-tournament RESOLVE (first gate-passer wins, no synthesis) -> dissent CAPTURE. Grep across aal/src, core/src, adapters/, scripts/, test/ finds: no ANALYZE/blind-judge Deliberation stage (no consensus/contradictions/partial/unique/blind-spots structure anywhere); no resolvers

**Plan:** Smallest agent-doable close, keeping fusion OFF by default (calibration policy intact): (1) In aal/src/fusion.ts add an ANALYZE stage: `deliberate(adapter, candidates, artifactKind)` that sends blinded candidates (strip panelIndex/provenance, no implementer persuasion text) to one judge invoke (tools:[] proposal-only) and returns a typed `Deliberation { consensus: string[]; contradictions: string[]; partial: string[]; unique: string[]; blindSpots: string[] }`; wire it into a new `fusePipeline(adapter, req, artifactKind, panelSize, runners)` that calls existing PANEL/EVIDENCE then ANALYZE then per-kind RESOLVE then CAPTURE. (2) Add a resolver map keyed by artifact kind: `code` = existing evidence-tournament (unchanged, judge never overrules gate); `plan`/`adr` = deliberate-synthesis (one synthesis invoke from Deliberation, result must pass caller-supplied planning-gate runner — reuse the Phase 4 planning gate); `tests` = union with dedupe (normalized test name+body hash) + per-test RED-check via caller-supplied runner, drop tests that pass on RED; `hypotheses` = union + stable sort by estimated probe cost (probes.length or provided cost field); `reviews` = weighted ensemble merge where contradictions escalate (mark `escalate: true`) instead of majority-vote suppression. (3) Entry points: (a) implement a `fusion.deliberate` tool handler in the AAL action/REQUEST_TOOL path with a budget cap (max panelSize from policy, hard costMultiplier cap) and `depth <= 1` guard (thread a depth field through the request; reject nested deliberate calls) — this also makes conformance P5 exercise a real tool; (b) add a thin virtual `fusion:<base-adapter>` Adapter wrapper that runs fusePipeline around a registered base adapter; (c) add policy-trigger config (e.g. .ai/policy fusion section: planning=always-eligible, riskLevel>=L2, repeated_failure) that the router consults, with all triggers defaulting OFF per fusion-measure calibration. (4) Unit tests with a stub adapter for each resolver, the RED-check drop path, depth-guard rejection, and budget cap; run existing suite. (5) Append a DEVIATIONS/DECISIONS entry noting the pipeline is now built for all artifact kinds but enablement remains OFF pending calibration uplift (n>1), commit on build/platform branch per D-005.

## G47 [§7.6] — `adapters/codex.ts` (Codex — sandbox เป็น execution backend ทางเลือก)

**Why:** Confirmed missing with no documented cut. (1) Spec is normative for built phases: unified-platform-spec.md §7.6 line 316 lists `adapters/codex.ts` as one of four Phase 1 (Ring 2) adapters, line 516 repeats it in the §14 repo structure, and the Phase 3 exit criteria (line 544) require "adapters ครบ (Claude/Codex/GLM-5.2)" — PROGRESS.md marks Phase 3 DoD passed 2026-07-05. (2) Not implemented: /Users/king_developer/Desktop/Project/autonomous-engineering-platform/adapters/src/ contains only anthrop

**Plan:** Follow the exact D-007 pattern already used for openai-compatible.ts. (1) Create /Users/king_developer/Desktop/Project/autonomous-engineering-platform/adapters/src/codex.ts modeled on openai-compatible.ts: header comment "UNVERIFIED until conformance P1-P8 runs with real credentials (D-007)"; export class CodexAdapter implements Adapter (from @platform/aal); manifest() returns honest values with adapterId:'codex' and executionBackend:true (the spec §7.6 differentiator — "sandbox เป็น execution backend ทางเลือก"; the CapabilityManifest field already exists per _template.ts); invoke() as thin wire-format translation only (INV-8/INV-9: schema-in-prompt, no tool definitions), reading credentials from an env-var name option (e.g. codexApiKeyEnv / CODEX_API_KEY) never hardcoded, with requestId idempotency cache (P8), 429 → throw QuotaLimitError without self-retry, and usage.interactionMode labeling — same checklist as _template.ts. (2) Add `export * from './codex.js';` to adapters/src/index.ts. (3) Mirror whatever unit-test coverage openai-compatible.ts has (typecheck must pass; do NOT register the adapter — registration waits on `scripts/run-conformance.mjs --adapter codex` with real credentials, same as D-007). (4) Amend docs/DECISIONS.md D-007 (or add a new D-entry) to include codex.ts in the written-unverified list, and update PROGRESS.md Phase 3 line + pending-human item 4 to mention codex alongside openai-compatible. No human input needed for any of these steps.

## G48 [§7.6] — aggregator เช่น OpenRouter = ผู้ประมวลผลข้อมูลอีกราย → `provider_data_policy` ต้องระบุ path ที่อนุญาต

**Why:** Spec §7.6 (unified-platform-spec.md:316) says provider_data_policy "ต้องระบุ path ที่อนุญาต" for aggregator baseUrls — normative "must". adapters/src/openai-compatible.ts implements no such field: OpenAICompatOptions has only baseUrl/apiKeyEnv/model, and invoke() (lines 50-53) sends ALL contextBundle pieces to the aggregator unfiltered. Repo-wide grep confirms provider_data_policy exists only in the spec and one code comment (line 8). No equivalent mechanism elsewhere (core context-builder secre

**Plan:** 1) In adapters/src/openai-compatible.ts add to OpenAICompatOptions: `providerDataPolicy: { allowedPathPrefixes: string[] }` (required — spec says aggregator registration must specify allowed paths; ContextBundle pieces already carry `path?` per aal/src/protocol.ts:14, no protocol change needed). 2) In invoke(), before building the prompt, filter contextBundle.pieces fail-closed: drop any piece of kind 'file'/'diff' whose path is undefined or does not start with an allowed prefix (normalize with posix path prefixes); record dropped ids/count in adapterMeta so the manifest diff is auditable. 'doc'/'guidance' pieces (core-authored, no path) pass through. 3) Add a vitest test (co-located, picked up by root vitest.config.ts) that stubs global fetch, invokes the adapter with one allowed and one disallowed file piece, and asserts the disallowed content never appears in the request body and the allowed one does. 4) One-line note in docs/DECISIONS.md D-007 that provider_data_policy enforcement is implemented (path-prefix allowlist) and exercised by unit test, still pending live conformance.

## G49 [§8 F-Hook] — F-Hook: builder/validator ทุก event + 5 handler types + scope + disableAllHooks + consent gate

**Why:** Confirmed: console/backend/src/extensions.ts:94 hard-codes hooks[].type='command' — the PUT /api/hooks/:scope body (line 81) has no handler-type field and the web.ts hooks page has no type selector, so only 1 of the spec's 5 handler types is buildable. disableAllHooks is read-only (extensions.ts:64 in GET /api/hooks) with no write endpoint or UI toggle anywhere in the repo (grep confirms). The gap is not on the pending-human list (PROGRESS.md:97-103), not in the deliberate Phase 4 cuts (PROGRESS

**Plan:** 1) Extend PUT /api/hooks/:scope in console/backend/src/extensions.ts: replace the flat {event,matcher,command,consent} body with {event,matcher,handler:{type,...},consent}; validate handler.type against an allowlist derived from the pinned Claude Code hooks schema (command + prompt/agent/etc. — enumerate from the CLI docs/sdk.d.ts of the pinned version) with per-type required-field checks (e.g. command→command:string, prompt→prompt:string); keep 428 consent gate and audit. Preserve backward compat by mapping a bare `command` field to {type:'command'}. 2) Add PUT /api/hooks/:scope/disable-all with body {disabled:boolean|null, consent:true} that sets/deletes settings.disableAllHooks via the existing atomicWrite + audit({type:'HOOK_WRITE',...}). 3) Update web.ts pages.hooks: handler-type <select> that swaps the input fields per type, plus a per-scope disableAllHooks toggle (with confirmDialog + consent checkbox). 4) Tests in console/backend/test/extensions.test.ts: one write per handler type asserting valid settings.json shape, reject unknown type (400), disableAllHooks set/unset round-trip, consent 428 for both endpoints. 5) If the pinned CLI supports fewer than 5 handler types, add a DEVIATIONS.md entry (pattern of DEV-001) recording the actual supported set.

## G50 [§8 F-Loop] — 

**Why:** Confirmed. Spec §8 line 343 makes F-Loop P0/Phase 3 with four required read-only views: task graph + state machine + latest calibration + approval packages. Only approval packages are implemented. core/src/human.ts exposes exactly /approvals, /approvals/{id}, /events, /steer, /kill; console/backend/src/loop-console.ts proxies only those; web.ts pages.loop (line 796) renders approval queue + raw event lines + steer + kill. Zero occurrences of task-graph/state-machine/calibration anywhere in conso

**Plan:** 1) core/src/human.ts: extend HumanPlaneDeps with optional `snapshot?: () => Array<{id: string; state: string; dependsOn?: string[]}>` and `calibrationDir?: string`; add `GET /tasks` returning `{ tasks: deps.snapshot?.() ?? [] }` and `GET /calibration` returning the newest .json in calibrationDir by mtime as `{ file, data }` (404 when none), behind the same bearer-token auth. 2) Wire `snapshot` where startHumanPlane is constructed (orchestrator/run scripts): derive per-task latest state + dependsOn from the existing event log (last transition event per taskId) so no orchestrator refactor is needed; pass `calibrationDir: '.ai/calibration'`. 3) console/backend/src/loop-console.ts: add `GET /api/loop/tasks` and `GET /api/loop/calibration` proxies following the existing /approvals pattern. 4) console/backend/src/web.ts pages.loop: add a "Task graph & states" card (table: task id, state pill, dependsOn list) refreshed inside the existing refresh(), and a "Latest calibration" card (file name + pretty-printed JSON, esc()'d), both read-only. 5) Tests: core/test/human.test.ts — /tasks returns wired snapshot, /calibration picks newest file and 404s when empty, both 401 unauthenticated; console/backend/test/phase3.test.ts — new proxy routes forward status+body. All doable without human input; live calibration files already exist for a real render check.

## G51 [§8 F-MCP] — F-MCP: จัดการ 3 ชั้น (user/project/managed-ro) + add stdio/HTTP + test connection + enable/disable + OAuth

**Why:** Confirmed. extensions.ts:34-55 implements only project-scope .mcp.json GET/PUT (stdio/HTTP add); web.ts pages.mcp (lines 648-665) is an add-only UI explicitly labeled "project scope". No user-scope (~/.claude.json) layer, no managed read-only layer, no test-connection endpoint, no enable/disable toggle, no OAuth flow anywhere in the repo (grepped core/, aal/, adapters/, scripts/, test/, console/ excluding node_modules). F-MCP is normative for built phases: spec line 540 puts it in the Phase 2 Co

**Plan:** Extend console/backend/src/extensions.ts F-MCP block (plus web.ts pages.mcp UI and extensions.test.ts), all agent-doable: (1) Scopes — add ?scope=user|project|managed to GET/PUT /api/mcp: user scope reads/writes mcpServers in ~/.claude.json (atomicWrite, preserve other keys); project stays .mcp.json; managed reads /Library/Application Support/ClaudeCode/managed-settings.json (per-OS path) and PUT to managed returns 403 read-only. GET returns all three layers plus a merged effective view with provenance (managed > user > project precedence per CLI docs). (2) Enable/disable — POST /api/mcp/:name/toggle writing enabledMcpjsonServers/disabledMcpjsonServers arrays in <dir>/.claude/settings.local.json (the real CLI mechanism for .mcp.json servers); GET annotates each server with enabled state. (3) Test connection — POST /api/mcp/test with a server def: stdio → spawn command/args, send JSON-RPC initialize over stdin, await response with 5s timeout then kill; http/sse → fetch url with MCP initialize POST (fallback HEAD), report ok/status/latency/needsAuth (401 + WWW-Authenticate detected). (4) OAuth — minimal RFC-compliant flow for remote servers: on 401, do protected-resource metadata discovery (RFC 9728) + authorization-server metadata (RFC 8414), dynamic client registration, generate authorization URL with PKCE, GET /api/mcp/oauth/callback endpoint exchanging code for tokens stored (0600) under ~/.claude/.platform-mcp-tokens.json; surface per-server auth status in UI; test the whole flow in vitest against an in-process mock OAuth+MCP server. Add UI: scope picker, enable/disable toggle per server, Test button showing result, Authenticate button. Tests for each endpoint incl. managed-ro 403, toggle round-trip, test-connection success/timeout/401, oauth mock flow. Record in docs/DECISIONS.md that managed scope path is OS-dependent and OAuth tokens are console-managed (separate from CLI keychain).

## G52 [§8 F-Skill] — F-Skill: จัดการ skills + enabledPlugins + marketplace

**Why:** ยืนยันว่า gap จริง F-Skill (spec บรรทัด 340: "จัดการ skills + enabledPlugins + marketplace", P2 Phase 3) ถูก implement แค่บางส่วน: (1) console/backend/src/extensions.ts:123-133 GET /api/skills ทำแค่ readdirSync คืนชื่อ directory (user + project) — ไม่อ่าน SKILL.md metadata และไม่มี endpoint จัดการ plugins ใดๆ (grep "enabledPlugins|marketplace" ทั้ง console/core/docs พบเฉพาะข้อความ note ใน web.ts:726); (2) web.ts:716-727 pages.skills เป็น read-only pills + การ์ด Plugins ที่เป็นข้อความ punt ไป Set

**Plan:** ปิดแบบเล็กสุดโดยไม่ต้องมีมนุษย์: (1) extensions.ts — ขยาย GET /api/skills ให้อ่าน frontmatter name/description จาก SKILL.md ของแต่ละ skill dir; เพิ่ม GET /api/plugins คืน enabledPlugins จาก settings ทุก scope (user/project/local — reuse ตัวอ่าน settings ที่ F-Set ใช้) + รายชื่อ marketplace ที่รู้จักจาก ~/.claude/plugins/ (อ่านอย่างเดียว, existsSync guard); เพิ่ม PUT /api/plugins body {scope, name, enabled} แก้ key ใน enabledPlugins ของ settings ไฟล์ scope นั้นด้วย pattern เดิมของ repo (validate → atomicWrite → audit event PLUGIN_TOGGLE เหมือน /api/system/retention); (2) web.ts pages.skills — แสดง skills พร้อม description, การ์ด Plugins เป็นตารางต่อ scope + toggle enable/disable (เรียก PUT) + การ์ด Marketplaces read-only; (3) marketplace install/uninstall ยังทำผ่าน CLI → บันทึกเป็น documented decision ใน docs/DECISIONS.md (Console เป็น read-only view ของ marketplace, install เป็น interactive CLI flow); (4) vitest ใน console/backend: GET /api/plugins รวม scope ถูกต้อง, PUT เขียน enabledPlugins ลงไฟล์ scope ที่เลือกและ reject ชื่อ/scope ไม่ valid, GET /api/skills คืน description จาก SKILL.md

## G53 [§8 F-Status] — F-Status: เวอร์ชัน CLI, doctor ย่อ, active runs (ทั้งสองโหมด), การ์ด auth + quota ย่อ

**Why:** Confirmed partial. Spec §8 line 328 requires F-Status to show "doctor ย่อ" and "active runs (ทั้งสองโหมด)". web.ts pages.status (lines 363-392) shows CLI version, auth card+warnings, quota card, and interactive PTY terminals only; autonomous runs are just a muted pointer to Loop Console (line 378), and F-Loop itself only shows approvals/events (loop-console.ts proxies /approvals,/events,/steer,/kill — no runs list; core/src/human.ts has no /tasks endpoint either). No mini doctor exists on Status

**Plan:** Smallest close, no human input needed. (1) Backend: in console/backend/src/extensions.ts factor the existing doctor exec (lines 135-143) into a shared runDoctor() with a ~5-10s timeout and a 5-min in-memory cache; in server.ts extend GET /api/status (line 139) to add doctor: {ok:boolean|null, summary:string} from the cached runDoctor(), and loop: {reachable:boolean, pendingApprovals:number} by probing the Human Plane GET /approvals with the same token-file logic as loop-console.ts (short timeout, reachable=false on error). (2) Frontend: in web.ts pages.status add a small "Doctor (brief)" card rendering st.doctor.summary with a link to #/sys, and split the "Active runs" card into Interactive (existing PTY table) and Autonomous — render loop reachable/pendingApprovals plus running jobs from GET /api/sched (endpoint already exists, loop-console.ts:88), with empty state "loop not running" and a link to #/loop. (3) Tests: extend console/backend/test/server.test.ts (or phase5-console.test.ts) to assert /api/status now includes doctor and loop fields with correct shape when the Human Plane is unreachable, and that the sched-jobs path returns a running job after /api/sched start. Roughly 3 source files + tests; follows existing patterns (audit not needed for read-only status).

## G54 [§8 F-Sub] — F-Sub: CRUD (Markdown+frontmatter) + test run

**Why:** Confirmed. Spec §8 (unified-platform-spec.md:339) requires F-Sub = CRUD + test run, Phase 3 (built). extensions.ts:100-121 implements only GET (list/read) and PUT (create/overwrite) of .claude/agents/*.md — no DELETE endpoint exists in the backend (only app.delete is /api/term/:id), and no test-run capability exists anywhere in console/, core/, aal/, adapters/, or scripts/. Not on any documented cut: DEVIATIONS.md has only DEV-001, DECISIONS.md never mentions F-Sub, PROGRESS.md's documented cuts

**Plan:** Smallest agent-doable closure, all in console/backend: (1) DELETE /api/subagents/:name in src/extensions.ts — validate /^[a-z0-9-]+$/, 404 if file missing, unlinkSync join(dir,'.claude','agents',`${name}.md`), audit {type:'SUBAGENT_DELETE'}, return {ok:true}. (2) Test run: POST /api/subagents/:name/test with body {input:string} — read the agent file, parse frontmatter (name/description/tools), then run a one-shot non-interactive invocation via execFile('claude', ['-p', input, '--agents', JSON.stringify({[name]:{description, prompt, tools}}), '--max-turns','3'], {timeout:60_000}); since >5s, register it through the existing background-action pattern (action + poll GET /api/actions/{id}/status per §8) and return {actionId}; audit {type:'SUBAGENT_TEST'}. (3) web.ts subagents page: per-row Delete button using the existing confirm-modal helper, and a Test run button (input field + output pre, polling action status). (4) Tests in console/backend/test/extensions.test.ts: DELETE removes file + rejects bad name (400) + 404 on missing; test endpoint validates input and returns an action id (stub execFile). Optional stretch for full F-Sub wording: mirror the same GET/PUT/DELETE trio for .claude/commands/*.md as /api/commands. Verify: pnpm test green, then live-check the page renders and delete/test-run round-trip works.

## G55 [§8 F-Sys] — F-Sys: doctor เต็ม, update, host stats, retention (cleanupPeriodDays, ลบ transcript + คำเตือนข้อมูลอ่อนไหว)

**Why:** Confirmed partial. Spec §8 line 345 requires F-Sys = full doctor + update + host stats + retention, and Phase 4 / "Console SPA เต็มตาม §8" is declared done, so it is normative. Host stats + retention (warning + confirm + audit + test) are fully implemented (console/backend/src/extensions.ts:136-170, web.ts pages.system ~line 873-895, test/phase5-console.test.ts:171). But (a) `update` is absent everywhere — no endpoint, no UI, no `claude update` invocation in console/, scripts/, or tests, and it 

**Plan:** Two small changes, no human input needed. (1) `update`: add POST /api/system/update in console/backend/src/extensions.ts — require body `{ confirm: true }` (else 428, matching the existing consent-gate pattern), then `pExecFile('claude', ['update'], { timeout: 180_000 })`, return `{ ok, output }` (catch → 502 with stderr message), and `audit({ type: 'SYSTEM_UPDATE', ts })`. In web.ts pages.system add a "Run update" button in the Doctor/Host card wired through the existing confirmDialog + savedToast helpers. Write the test first (repo rule 4) in console/backend/test/extensions.test.ts: POST without confirm → 428; with confirm on a machine where execFile fails → 502 path (deterministic, no real update run in CI). (2) doctor deviation: append DEV-002 to docs/DEVIATIONS.md — spec §8 expects "doctor เต็ม" but installed CLI's `claude doctor` is interactive-only / has no stable --json output, so GET /api/system attempts `claude doctor --json` and falls back to directing the operator to run /doctor inside F-Term (extensions.ts:137-139); note this preserves full doctor capability via F-Term parity (INV-17). Rebuild dist, run CI (85 tests) green, commit on build/platform.

## G56 [§8 F-Usage] — 

**Why:** Spec §8 line 341 + §5.3 line 186 normatively require the Quota HUD to show (a) reset time (weekly per user-entered reset cycle; 5h window reset is derivable from windowStart), (b) weekly all-models vs Sonnet-only counters, and (c) a calibration input where the user enters observed % from official /usage. None exist: windowStart is computed in console/backend/src/usage.ts:69 but never rendered in web.ts pages.usage (lines 744-757); week is a single all-models total (usage.ts:76); grep "calibrat" 

**Plan:** Smallest close, no human input needed (all data local; reset/observed-% are user-entered runtime fields per spec §5.3): (1) console/backend/src/usage.ts — add windowResetAt = windowStart + WINDOW_MS to UsageEstimate; add pure helpers weeklySplit(aggregates, now, weeklyResetISO?) returning { all, sonnet } by filtering models matching /sonnet/i over the current weekly cycle (fall back to rolling 7d when no reset entered), and applyCalibration(estimate, observed) that stores observed window/week % and derives a scale note. (2) console/backend/src/server.ts — include windowResetAt and week split in GET /api/usage (reuse search.aggregates like /api/usage/full); add GET/PUT /api/usage/calibration persisting { weeklyResetISO, observedWindowPct, observedWeekPct } to the existing console state dir (.ai/console/, small JSON file). (3) console/backend/src/web.ts pages.usage — render "window resets ~<time>" from windowResetAt, split the Week card into "all models" and "Sonnet" KPIs, and add a small Calibration card: inputs for weekly reset time + observed % (deep-linking officialSource), PUT then re-render showing estimated vs observed side by side, keeping the estimate label (INV-13). (4) Tests — extend the existing usage unit tests (vitest, repo test setup) for windowResetAt math, sonnet split counting incl. no-reset fallback, and calibration persistence round-trip via the API; run CI green before commit on build/platform.

## G57 [§9.1/§9.2 ข้อ 3] — Loop 3 Task/TDD: RED → GREEN → REFACTOR → VERIFY → REPAIR · REFACTOR: หลังเขียวเท่านั้น → core รันเทสต์ทั้งหมดอีกครั้ง

**Why:** ยืนยัน gap จริง: grep "refactor" ทั่ว repo (core/aal/adapters/console/scripts/test/docs) เจอเฉพาะใน spec — ไม่มี implementation/test เลย; core/src/orchestrator.ts:68-71 หลัง T1 pass ไป VERIFYING→REVIEWING ทันที ไม่มีรอบ propose หลังเขียวให้ agent เสนอ refactor และ core ไม่รันเทสต์ทั้งหมดซ้ำหลัง refactor; ไม่อยู่ใน documented cut ใด ๆ (DEVIATIONS มีแค่ DEV-001, DECISIONS D-001..D-008 ไม่เกี่ยว, PROGRESS ตัดเฉพาะ F-Chat/themes/i18n/canary deploy, §16 "ตัดก่อนได้" ไม่มี REFACTOR) และไม่อยู่ใน pendi

**Plan:** แก้เล็กสุด ไม่ต้องมี state ใหม่ (คง §6.3 เดิม): (1) core/src/types.ts เพิ่ม field optional ใน AgentRequest: `phase?: 'implement' | 'refactor'` (2) core/src/orchestrator.ts หลัง `t1.status === 'pass'` (บรรทัด 68) ก่อน transition ไป REVIEWING: เรียก `agent.propose({ taskId, role, iteration, feedback, phase: 'refactor' })` หนึ่งรอบ (ยังอยู่ใน VERIFYING) — ถ้า actions ว่าง → log event REFACTOR_SKIPPED แล้ว REVIEWING ตามเดิม; ถ้ามี actions → executor.execute แล้ว core รัน runGate T0+T1 ใหม่ทั้งชุด (T1 = full unit+integration+golden ตาม §6.4 คือ "เทสต์ทั้งหมด" ที่มีใน tier ปัจจุบัน) log GATES_RUN payload `{refactor: true}` — ผ่าน → REVIEWING พร้อม gateConfigHash ใหม่; ไม่ผ่าน → เข้า path เดิม FAILED→DIAGNOSING→REPAIRING (budget backstop เดิมคุมอยู่แล้ว) (3) เพิ่ม test ใน core/test (stub agent แบบ fault-injection.test.ts): เคส a) agent ตอบ refactor ว่าง → จบที่ REVIEWING; เคส b) agent เสนอ refactor ที่ทำเทสต์พัง → core จับได้จากการรันซ้ำเองและเข้า repair loop (พิสูจน์ INV-1 ว่าไม่เชื่อ claim); เคส c) refactor ผ่าน → REVIEWING พร้อม evidence รอบใหม่ (4) รัน CI เดิม (vitest) ยืนยัน 85 tests เดิมไม่แตก

## G58 [§9.2 ข้อ 1] — RED: test agent เสนอเทสต์ → core รันและตรวจว่า fail ด้วยเหตุผลที่คาด — ผ่านตั้งแต่แรก = เทสต์อ่อน → ส่งกลับแก้เทสต์ก่อน

**Why:** ยืนยันครบ: §9.2 ข้อ 1 เป็น normative (และ §7.5 ต้องใช้ RED-check ต่อเทสต์ด้วย) แต่ state-machine.ts ไม่มี state สำหรับ red phase, orchestrator.ts:24 เข้า IMPLEMENTING ทันทีโดยไม่มีขั้น core รันเทสต์ที่ agent เสนอเพื่อตรวจ fail-first/fail ด้วยเหตุผลที่คาด และไม่มี logic ตรวจเทสต์อ่อน (ผ่านตั้งแต่แรก→ส่งกลับ) ที่ไหนใน repo; role test_designer ประกาศใน types.ts:3 + policy.ts:13 แต่ไม่มีผู้เรียกใช้; bridge.ts:37 hardcode implementer; visible tests ใน run-supervised-loop.mjs/run-semiauto-loop.mjs เขี

**Plan:** ขั้นเล็กสุดที่ agent ทำได้เอง: (1) เพิ่ม core/src/red-check.ts — ฟังก์ชัน runRedPhase({proposedTests:[{path,content,expectedFailurePattern?}], runner}) ให้ core เขียนเทสต์ลง test/ai-generated ผ่าน executor (policy ของ role test_designer มีอยู่แล้วใน policy.ts) แล้วรัน T0: ถ้าเทสต์ผ่านทั้งหมดตั้งแต่แรก → คืน {verdict:'WEAK_TEST'} เป็น structured feedback ส่งกลับ test agent; ถ้า fail แต่ expectedFailurePattern ไม่ match output → {verdict:'WRONG_REASON'} ส่งกลับเช่นกัน; fail ตรงคาด → {verdict:'RED_OK'} + log event RED_CHECK ลง event log. (2) ต่อเข้า orchestrator.ts: รับ optional testProposer (Agent role test_designer) ก่อนลูป implementer — วน red-check สูงสุด N รอบ, RED_OK แล้วจึง transition IMPLEMENTING; ไม่มี testProposer = ข้าม (backward compatible กับสคริปต์เดิม). (3) aal/src/bridge.ts: เพิ่ม makeTestDesignerAgent (agentRole:'test_designer', outputSchema {tests:[{path,content,expectedFailure}]}) คู่กับ implementer เดิม. (4) เพิ่ม unit tests ใน core: เทสต์อ่อน(ผ่านทันที)→WEAK_TEST+ส่งกลับ, fail ผิดเหตุผล→WRONG_REASON, fail ตรงคาด→เข้า IMPLEMENTING, และ event RED_CHECK ถูก append. (5) อัปเดต run-semiauto-loop.mjs ให้ใช้เส้นทาง test-designer จริงหนึ่งรอบเป็น proof, บันทึกผลใน PROGRESS.md — ไม่ต้องมี input จากมนุษย์

## G59 [§9.2 ข้อ 4] — REVIEW: reviewer ได้รับเฉพาะ Goal/AC/diff/evidence — ห้ามส่งคำอธิบายโน้มน้าวจาก implementer

**Why:** §9.2 rule 4 is normative ("TDD Rules (normative)") and the repo claims Phase 0-4 complete, but role 'reviewer' exists only as a type/policy string (core/src/types.ts:3, core/src/policy.ts:15,50, aal/src/protocol.ts:20). aal/src/bridge.ts builds requests only with agentRole 'implementer' and even injects implementer feedback/notes into the context. core/src/merge-policy.ts:31 returns 'needs-review' for L2 ("requires 2-lineage AI review + CI") but no consumer runs a review — orchestrator stops at 

**Plan:** Smallest close, no human input needed: (1) core/src/review.ts — vendor-neutral `buildReviewPackage(log, task)` producing exactly {goalExcerpt, acceptanceCriteria, diff, evidenceRefs} from core-owned artifacts (event log + executor diff), by construction excluding Proposal.note/claim/feedback — mirror the ApprovalPackage field shape in core/src/human.ts. (2) aal/src/bridge.ts — add `adapterReviewerPort(adapter, reviewPackage)` invoking with agentRole:'reviewer', contextBundle = review package pieces only, outputSchema {verdict:'approve'|'changes_requested', findings:string[]}; policy.ts already denies commands for reviewer. (3) Wire the needs-review consumer: on decideMerge=='needs-review' for L2, core builds the package, runs reviewer(s), appends REVIEW_REQUESTED/REVIEW_VERDICT events, transitions REVIEWING→APPROVED or CHANGES_REQUESTED; with only 1 lineage available (D-007), record 1 AI review + escalate to human ApprovalPackage noting "second lineage pending D-007". (4) Tests: implementer proposal containing persuasive note/feedback → assert reviewer request contains none of it (field-whitelist assertion); L2 path emits review events and cannot reach APPROVED without a verdict; changes_requested → CHANGES_REQUESTED transition. (5) One-line PROGRESS.md note: 2-lineage degraded to 1-lineage+human until D-007 credentials.

## G60 [§9.3] — 

**Why:** Confirmed. runHypothesisProbes (core/src/repair.ts) implements §9.3 mechanics and has tests, but grep across the entire repo shows callers only in core/test/phase2.test.ts and core/test/phase4.test.ts. The real autonomous loop (core/src/orchestrator.ts:73-76) transitions FAILED→DIAGNOSING→REPAIRING as three no-op sm.transition calls with comment "next proposal is treated as the repair" — it never requests hypotheses from the agent, never runs probes, never enforces max_hypotheses_per_failure, an

**Plan:** Smallest agent-doable fix, all inside core (vendor-neutral, no human input needed): (1) core/src/types.ts — add optional `hypotheses?: Hypothesis[]` to Proposal (import/move the Hypothesis type from repair.ts into types.ts to avoid a cycle), and add `'diagnostician'` to the core Role union to match aal/src/protocol.ts. (2) core/src/orchestrator.ts — replace the pass-through at lines 73-76: after sm.transition('FAILED'...) transition to DIAGNOSING, then call agent.propose({taskId, role: 'diagnostician', iteration, feedback}) where feedback carries the t1 failure detail; if the proposal returns hypotheses, call runHypothesisProbes(this.log, ctx.taskId, ctx.worktree, proposal.hypotheses, 3): on confirmed → sm.transition('REPAIRING') and continue the loop (next proposal is the small patch per patchPlan, which re-enters the existing VERIFYING T0 path); on escalate → sm.transition('ESCALATED', 'core', { refuted }) and return. If the agent returns no hypotheses, keep the current legacy fall-through to REPAIRING so existing fault-injection tests stay green (log an event noting legacy path). (3) core/test — add an orchestrator-level test with a stub AgentPort that fails T1, then returns one refutable and one confirmable hypothesis; assert HYPOTHESIS_REFUTED/HYPOTHESIS_CONFIRMED events appear in the event log from the runTask flow, and a second test where all hypotheses are refuted asserting finalState=ESCALATED with refuted statements in the payload. (4) Run pnpm test/CI; note the wiring in PROGRESS.md checkpoint. No API breaks: hypotheses field is optional, adapters unaffected.

## G61 [§9.4] — โมเดลขาดอะไร → ขอผ่าน READ_FILE (นับ context miss) — อ้างถึงสิ่งนอก context โดยไม่เคยขอ → reject result

**Why:** §9.4 บังคับสองอย่างที่ยังขาด: (1) นับ context miss เมื่อโมเดลขอผ่าน READ_FILE — grep ทั้ง repo ไม่มี counter/event/metric ใด ๆ (คำว่า context miss ปรากฏเฉพาะใน unified-platform-spec.md:205,381); executor.ts:85 แค่คืน content (2) reject result เมื่ออ้างถึงสิ่งนอก context โดยไม่เคยขอ — context-builder.ts สร้าง manifest แล้วไม่มีโค้ดไหนใช้เทียบ reference ใน proposal เลย (orchestrator.ts รับ claim → รัน gate ตรง ๆ; policy.ts เป็น role path allowlist คนละเรื่อง) ไม่อยู่ใน DECISIONS.md/DEVIATIONS.md/p

**Plan:** เพิ่มใน core (ไม่ต้องมีมนุษย์): 1) สร้าง core/src/context-ledger.ts — class ContextLedger รับ manifest entries (paths) ตอนเริ่ม task, มี grant(path) กับ has(path) และ missCount 2) executor.ts case READ_FILE: เมื่อ path ไม่อยู่ใน ledger → log event CONTEXT_MISS (principal core, payload {actionId, path}) + ledger.grant(path) ก่อนคืน content 3) orchestrator.ts: ก่อนรับ claim GREEN/READY_FOR_VERIFICATION ตรวจทุก action ใน proposal ที่แก้ไฟล์เดิม (WRITE_FILE/APPLY_PATCH ที่ path มีอยู่บนดิสก์) — ถ้า path ไม่อยู่ใน ledger → ไม่เข้า VERIFYING, ส่ง structured feedback {status:'rejected', reason:'references file outside context bundle never requested via READ_FILE', policy:'context-manifest'} + log CONTEXT_VIOLATION แล้ววน loop ต่อ (ไม่ crash ไม่เงียบ ตาม §6.1) 4) ต่อ TaskContext ให้ถือ ledger ที่ seed จาก buildContext().manifest.entries 5) tests ใน core/test/context-ledger.test.ts: READ_FILE นอก bundle → CONTEXT_MISS ถูกนับ+grant; claim ที่แก้ไฟล์เดิมนอก bundle โดยไม่เคยขอ → rejected feedback + ไม่เข้า VERIFYING; ไฟล์ใหม่ (ไม่มีบนดิสก์) เขียนได้ปกติ — ทั้งหมด deterministic รันใน CI เดิม

## G62 [§9.4] — วัดด้วย context recall + context waste

**Why:** §9.4 (Phase 1, เฟสที่สร้างแล้ว) กำหนด "วัดด้วย context recall + context waste" และ "นับ context miss ผ่าน READ_FILE" แต่ grep ทั่ว repo (core/, aal/, adapters/, console/, scripts/, spikes/, test/, docs/, .ai/) ไม่พบ metric ทั้งสองตัวใน source ใดเลย; core/src/context-builder.ts สร้าง manifest แต่ไม่คำนวณ metric, core/src/executor.ts:85 handle READ_FILE โดยไม่นับ miss/ไม่ log event, event log ไม่มี event ชนิดนี้, Console ไม่แสดง; ไม่อยู่ในรายการ "ตัดก่อนได้" ของ §16, ไม่มีใน docs/DEVIATIONS.md (มี

**Plan:** ขั้นต่ำสุดโดยไม่ต้องรอมนุษย์: (1) เพิ่ม core/src/context-metrics.ts ฟังก์ชัน pure computeContextMetrics(manifest, usedPieceIds, missCount) → { recall, waste, misses, providedBytes, usedBytes } โดยนิยาม recall = used/(used+misses) และ waste = bytes ของ piece ที่ไม่ถูกใช้ / providedBytes (used = piece ที่ path ถูกอ้างใน action ของ agent หรือปรากฏใน diff/result); (2) ใน core/src/executor.ts case READ_FILE: รับรายการ manifest piece paths ผ่าน TaskContext — path ที่ไม่อยู่ใน manifest = context miss → append event CONTEXT_MISS ลง event log; (3) ใน orchestrator ตอน task จบ: คำนวณ metric แล้ว append event CONTEXT_METRICS {taskId, recall, waste, misses, providedBytes, usedBytes} — Console เห็นผ่าน events feed ที่มีอยู่แล้ว ไม่ต้องสร้างหน้าใหม่; (4) เพิ่ม unit test ใน test/: กรณี no-miss/full-use (recall=1, waste=0), มี miss (recall ลด), piece ไม่ถูกใช้ (waste เพิ่ม), และ READ_FILE นอก manifest ผลิต CONTEXT_MISS event; core ยังปลอด vendor name ตาม INV-7

## G63 [§9.4] — Pipeline 6 ขั้น (deterministic): SEED → EXPAND (dependency graph, depth budget) → COMPRESS (symbol level) → ...

**Why:** Confirmed partial gap. Spec §9.4 (unified-platform-spec.md:379-380) is normative for Phase 1, which PROGRESS.md marks complete ("context builder 6 ขั้น"). In core/src/context-builder.ts: EXPAND (lines 55-86) has no dependency-graph traversal and no depth budget — it only ingests caller-provided seeds/docs, and all callers (scripts/run-supervised-loop.mjs:65, run-semiauto-loop.mjs:41, run-fusion-measure.mjs:45) pass hardcoded seed lists; COMPRESS (line 73) is a raw byte-slice truncation, with the

**Plan:** Implement both missing halves deterministically in core/src/context-builder.ts (no vendor names, no new deps — keeps INV-7 and dependency rules): (1) EXPAND: add `depthBudget?: number` (default 2) and `maxTotalBytes?: number` to ContextBuildInput; after reading each seed, regex-extract relative import specifiers (import/export ... from './x', require('./x'), dynamic import('./x')), resolve against the worktree with candidate extensions (.ts/.tsx/.js/.mjs/.cjs + /index.*), BFS in sorted-specifier order up to depthBudget, dedupe visited paths, stop at maxTotalBytes; every discovered file passes through the existing worktree-escape check, FORBIDDEN_BASENAMES/agentConfigFiles bar, secret-scan block, and COMPRESS; record depth per manifest entry. (2) COMPRESS: for oversized code files (.ts/.tsx/.js/.mjs/.cjs), replace the blind byte-slice with line-based symbol-level compression — keep import/export lines and top-level declaration signatures (function/class/interface/type/const/enum), elide bodies with a "[body elided — symbol-level compress]" notice; fall back to the existing truncation for non-code files; mark compressed entries in the manifest. (3) Extend core/test/context-builder.test.ts: chain a→b→c with depthBudget=1 includes b not c; expanded file containing a secret is blocked; expanded file resolving outside worktree is blocked; oversized .ts keeps signatures and drops bodies; manifestRef is byte-identical across two runs (determinism). (4) Update the two pipeline comments (lines 1-2, 72) to drop the "later" caveat, add a PROGRESS.md checkpoint note, and if any sub-behavior is still narrowed vs spec (e.g. import-regex EXPAND instead of full language-aware graph) record it as DEV-002 in docs/DEVIATIONS.md. (5) Run pnpm CI (all 85 tests + new ones) green; commit on build/platform per D-005 — no human input required.

